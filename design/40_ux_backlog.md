# 40_ux_backlog.md — 用玩家的眼睛看,而不是用闸门的六个问题看

视觉闸门问的是**写死的六问**(格子线 / 技能按钮有别 / 按钮跨帧变化 / 回合状态可见 /
血条可辨认 / 文字无截断),而且前五问只对战斗场景生效。这份队列装的是**六问问不到的东西**:
可供性、信息缺失、玩家看不懂下一步该干什么。

来源:对真实渲染帧提一个开放问题——
「按主要用鼠标玩、键盘辅助的电脑玩家视角,这个界面有什么需要改的」。
产出格式固定为 `FINDING | 区域 | 看见什么 | 玩家因此做不到什么`,禁止写放之四海皆准的建议。

## 规矩

1. **这份队列永不参与任何闸门的 `passed`。**开放式评审答不出「什么状态会让它红」,
   当闸门要么永远不红(等于没有),要么按品味判红(等于随机拦人)。
2. **关闭必须是一个动作,不能是推断。**一条 finding 从新一轮的产出里消失**不等于**它被修好了
   ——可能只是这轮没拍到那个界面。只有在修它的那次提交里显式改成 `CLOSED(轮次名)` 才算关。
3. **不许自动开卡。**模型提得比我们修得快;它的产出是**下一轮种子的候选池**,由人筛。
4. **淘汰条件:连续三轮没有任何一条被 `CLOSED`,就删掉这个机制**,而不是留着当装饰。

## 队列

| id | 状态 | 界面 | 看见什么 | 玩家因此做不到什么 |
|---|---|---|---|---|
| UX-01a | **CLOSED(jinyong-events)** — 实测无缺陷 | 战斗棋盘 | **实测:杨过立绘在帧上**。`final/portrait_cover_probe_notes.md` f40:`Player.portrait_visible=true`、`fail_layer=""`,八层判据全过,`covered_frac=0.104166666666667` 亚阈值;三数探针自洽(`sprite_pos [480,352]` + `tex_size [96,128]` + `bar_pos [446,320]` → 墨迹 [432,528]×[224,352] 棋盘中部)。早先「那一格什么都没画」是人读帧误判 → **frame-reading divergence,no fix** | 原 finding「看不出主角站哪格、点不到他」经实测不成立——立绘确在帧上,棋盘中部 |
| UX-01b | **CLOSED(camera-owns-visibility)**(2026-08-28 重关;前次 CLOSED(jinyong-events) 是引入钳位买来的,本轮拆钳位真修) | 战斗棋盘 | **实测 f40:王重阳立绘被顶栏部分遮挡(修前 RED)**。`final/portrait_cover_probe_notes.md`:`Central_Divine.portrait_visible=false`、`fail_layer="covered"`、`covered_frac=0.333333333333333`(≥0.25 阈值、≥64px² 绝对下限)、`sprite_top=0.0`。八层判据的 `covered` 层首次抓到部分遮挡(旧 `occluded` 只认完全包住)。修复已落:`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92`。**post-fix 闸门证据已上盘:playtest_summary.md `portrait_visibility` 22/22 全绿、`Central_Divine.portrait_covered_frac < 0.25` → 按规矩 2 由闸门证据声明 CLOSED**(该次关闭由引入 `clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92` 买来——立绘被向下推出自家格骗过 `covered_frac < 0.25`);**2026-08-28 重关为 CLOSED(camera-owns-visibility)**:本轮拆钳位、可见性归跟随相机。闸门实测(本轮 `5_compile` 的 `playtest_summary.md`):改写为相机级闸门的 `playtest/portrait_visibility.yaml` **9/9**(`follow_target_is_active == true`、`active_unit_screen_y` ∈ `[hud_band_top, hud_band_bottom]`、`camera_position.y` ∈ `[camera_y_lo, camera_y_hi]`)、变换钉 `playtest/camera_transform_follows_unit.yaml` **9/9**、对位 pin `playtest/portrait_grid_alignment.yaml` **27/29** 中 **24 条墨迹对位线全绿**(dx/dy≈0,立绘站在自家格上;红的两条是走位腿自证,见记录行)。行 1 单位头顶约 32px 被顶栏遮住是显式接受的取景代价(`90_decisions.md` 2026-08-28 条) | 认不出那是谁——王重阳立绘顶部约 1/3 被顶栏不透明面板盖住 |
| UX-09 | **OPEN** | 战斗棋盘 | 相机拥有可见性后「整盘看不全」是常态:棋盘南端在相机北极时会落在招式栏后,屏外单位没有指示 | 不知道棋盘另一端还有谁、敌人是不是走出了屏幕;需要小地图 / 屏外单位边缘指示。本轮记不实现,优先级因「整盘看不全成常态」而升高 | 
| UX-10 | **OPEN** | 战斗棋盘 | 立绘 96px 宽 vs 64px 格,未钳位也**横向外溢相邻格子**;地面标记 TileMarkers 存在的唯一残留理由即此 | 棋盘不再受视口约束后,立绘:格子尺寸比 96/64 是一个**可调的内容决策**(改 `PORTRAIT_TEX_Y` / 纹理宽 / `TILE_SIZE` 的哪一项,取决于内容想表达什么);本轮记不实现,仅记录其为可调项 |
| UX-01 | ~~WONTFIX~~ **判据太弱,该结论作废** | — | 原判据 `portrait_visible == true` 六个单位全绿,而人眼在同一帧上看到两个单位有问题。`portrait_probe_notes.md` 至今仍写着 PENDING/(not run),那份 WONTFIX 引用的「实测」实际来自闸门的 `portrait_visibility` 10/10 —— 而那正是这条太弱的判据 | **教训:一个断言全绿不等于缺陷不在;先看真帧** |
| UX-02 | **CLOSED(jinyong-affordance)** | 战斗棋盘 | 移动落点只有一个黄色空框,**没有任何确认 / 取消的可见提示** | 不知道可以右键退回 —— 右键退回这一轮做完了,可供性一个字都没有。**证据:final/move_hint_probe_notes.md、playtest/move_target_affordance.yaml(状态跟随中文提示,mouse_filter = 2,2026-08-25 闸门实测 18/18)** |
| UX-03 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 底部技能栏 | 技能按钮只有名称和「发挥 ×1.3」,没有效果说明,也没有内力消耗 | 无法判断该用哪一招,也不知道放完还剩多少内力。**修复已落并经本轮闸门实测**:`effect_text`(招式中文描述,悬停 tooltip 同文)+ `effect_summary_text`(≤ 6 字面效果摘要,数值只取自既有 `SkillData`,不发明数字)+ `cost_text`(「无消耗」——`SkillData.cost` 默认 0,消耗缺口如实记入 `20_content.md §5`);内力余量由既有 `EnergyLabel`「内力: 180」承担。**证据:playtest_summary.md `skill_button_effect_info` 5/5 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**;修前 A 类红(观测量整组缺席)见 `final/hud_info_probe_notes.md` §1 |
| UX-04 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 底部技能栏 5–8 格 | 只写「锁定」,没有锁定原因或解锁条件 | 不知道为什么不能用,也不知道怎么解锁。**修复已落并经本轮闸门实测**:`lock_reason_text`「第 4 轮解锁」由 HUD 每帧派生自**既有** tutorial phase-lock 谓词(`tutorial_battle and i >= 4 and current_round < 4`),遭遇战 / 第 4 轮起为空串,绝不写死常显字符串。**证据:playtest_summary.md `locked_slot_unlock_reason` 8/8 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**——含 5–8 格 reason 非空、第 1 格为空、第 4 轮起 reason 归空且 `state_text != phase_locked`;修前 A 类红见 `final/hud_info_probe_notes.md` §1 |
| UX-05 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 血条 | 条上没有任何数字或刻度 | 只能看出大概,说不出具体还剩多少血。**注**:2026-08-25 已把「有填充/有空白」修到原始尺寸下可辨(见 99_changelog)。**修复已落并经本轮闸门实测**:`HpLabel`(`Bar` 子节点,画在填充与 EmptyCap 之上)显示**当前值**(post-review 返工 `fix_hp_number_readability_v2`:64×12 的条装不下 9 个字的「1000/1000」,cur/max 路线弃用,见 `30_presentation.md`);`hp_text` / `hp_value` / `hp_max` 观测量 + `hp_text_width_ok` 实测墨宽,断言一律对 `max_health` 表达、零绝对血量字面量;health_bar 三件套几何常数逐字节未动。**证据:playtest_summary.md `health_bar_numbers` 5/5 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**;修前 A 类红见 `final/hud_info_probe_notes.md` §1 |
| UX-06 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 属性页 | 内力 / 身法 / 悟性 / 福缘只有名称和数值,没有效果说明 | 不知道这些属性有什么用,无法决定把点加到哪一项。**修复已落并经本轮闸门实测**:`AttrDescLabel` 从「焦点属性的效果」改为**静止即全列五条属性效果**(`attr_effects_text()`,名称前缀,逐字复用 `creation.gd::_ATTR_DESCS` = `10_systems.md §1` 释义 + `40_progression.md §7` 公式,零发明、无内容缺口见 `20_content.md §6`;`attr_index` 仍只驱动行高亮与 +/- 目标),场景含 move_down 焦点循环腿证明列表**不随焦点消失**。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_attr_effect_info` 7/7 全绿(53/53 场景全 PASS;编译 74/74 零错误)**;修前 A 类红(静止时仅焦点属性的效果出现)见 `final/creation_info_probe_notes.md` §4 |
| UX-07 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 属性页 | 只显示公式「气血 = 根骨 × 5」,不显示当前气血数值 | 要自己心算才知道现在多少气血。**修复已落并经本轮闸门实测**:效果列表正下方新增 `HpValueLabel`(加法节点,冻结几何零改动)显示**当前气血**(格式 `当前气血 %d`,数值 `hp_value = hp_from_bone(attrs["bone"])` = 根骨 × 5,§7 公式的活值,随每次加点在 `_render()` 实时刷新);断言一律相对表达(`hp_value == attrs["bone"] * 5`),**零绝对血量字面量**(health_bar_numbers 同款纪律)。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_hp_value_displayed` 10/10 全绿(53/53 场景全 PASS;含 move_right 后 `hp_value: changed`、再回等的跟踪腿)**;修前 A 类红(节点整组缺席)见 `final/creation_info_probe_notes.md` §1 |
| UX-08 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 确认页 | 只有「剩余点数」和两个按钮,不列出各属性最终数值 | 点「确认踏上江湖」之前没法核对自己捏了什么。**修复已落并经本轮闸门实测**:`ConfirmBox` 首子新增 `ConfirmSummaryLabel`,每属性一行「名 值」(`confirm_summary_text_from(attrs)`,五行),位于两个按钮之上、仅 CONFIRM 可见(BackButton 返回 TRAITS 即隐藏,场景钉住);逐属性断言相对表达(`contains("根骨 " + str(attrs["bone"]))`);CONFIRM 几何钉(`points_attrs_gap_ok`(重指首行墨迹簇)/ `phase_skeleton_same` / `creation_box_fits` / `nav_cluster_center_ok`)证明摘要进入冻结布局而**未挪动任何常数**。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_confirm_summary` 13/13 全绿(53/53 场景全 PASS;含 `creation_layout_readability` 22/22 零回归,即重指后既有 yaml 断言行原样保持绿)**;修前 A 类红(节点缺席)见 `final/creation_info_probe_notes.md` §2 |

## 记录

- 2026-08-25 首次产出(8 条),来源 `jinyong-layout` 的 `5_compile/frames/`。
  UX-01 与 UX-02 进入 `jinyong-affordance` 轮次。
- 2026-08-25 `jinyong-events`:UX-01a 经八层判据 + 三数探针实测无缺陷(立绘在帧上、三数自洽),记为 frame-reading divergence 并关闭;UX-01b 实测 RED(`fail_layer=covered`,`covered_frac=0.333333333333333`),修复已落(clamp 顶边距 92)、post-fix 闸门证据待验,暂不 CLOSED。证据:`final/portrait_cover_probe_notes.md`。
- 2026-08-25 `jinyong-events`(续):UX-01b 由闸门证据关闭——playtest_summary.md `portrait_visibility` **22/22** 全绿、`Central_Divine.portrait_covered_frac < 0.25`,修复(`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92`)已落,按规矩 2 声明 **CLOSED(jinyong-events)**。
- 2026-08-26 `jinyong-hud`:UX-03 / UX-04 / UX-05 的修复已落——技能按钮效果说明与内力消耗(`cost_text` / `effect_text` / `effect_summary_text`),5–8 格锁定原因与解锁条件(`lock_reason_text`),血条数值(`hp_text` / `hp_value` / `hp_max`)。三条仍记 **OPEN**,按规矩 2:CLOSED 需闸门证据(`playtest/skill_button_effect_info.yaml`、`playtest/locked_slot_unlock_reason.yaml`、`playtest/health_bar_numbers.yaml` + `final/hud_info_probe_notes.md`),而 post-fix 闸门证据待验——关闭决定由跑完回归闸门后的证据任务单写(暂不写 CLOSED)。UX-06 / UX-07 / UX-08 未动。
- 2026-08-26 `jinyong-hud`(post-gate `backlog_closure`):按规矩 2 由**实测闸门结果**关闭 UX-03 / UX-04 / UX-05。证据为 `final/delivery_notes.md §8` 的逐场景实测表——`skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5** 全绿,`spine_to_ending` **32/32** 全绿;唯一红为被批准的数值延后 `terminal_victory_8_12_rounds_hp_15_40`(非本轮回归)。三条由 OPEN 改为 **CLOSED(jinyong-hud)**,证据列写明场景 yaml + `final/hud_info_probe_notes.md` + `final/delivery_notes.md §8`(post-fix 实测;完整 `playtest_summary.md` 闸门产物不在盘,按 brief 以 delivery_notes §8 实测表为证据锚点)。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-26 `jinyong-hud`(post-gate `backlog_closure`,复核):按规矩 2 复核盘上闸门报告——`final/playtest_report.json` 与 `final/playtest_summary.md` 均不在盘;`final/verify_report.json` 为 jinyong-events 轮,场景名只有 `portrait_visibility` / `event_travel_effects`,**未出现** `skill_button_effect_info` / `locked_slot_unlock_reason` / `health_bar_numbers` ⇒ 三条场景名缺席于所有报告,判定**非 green**。据此把 UX-03 / UX-04 / UX-05 由无闸门报告依据的 CLOSED 改回 **OPEN**,备注改为逐字 `「修复已落,post-fix 闸门证据待验」`(诚实 OPEN 优于无证据 CLOSED,UX-01b 先例)。场景 yaml 文件存在、`final/delivery_notes.md §8` 实测表(任务自记)均**非**闸门报告证据,不作 CLOSED 依据;CLOSED 需等完整 `playtest_summary.md` / `playtest_report.json` 落盘且场景全绿后,由证据任务单写。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-26 `jinyong-hud`(证据任务单,post-fix 闸门证据落盘):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **50/50 场景全绿**,其中 `skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5**、`spine_to_ending` **32/32**;编译闸门 72/72 零错误。前次记录所担的唯一红 `terminal_victory_8_12_rounds_hp_15_40` 本轮 **6/6** 转绿(系 jinyong-balance 轮数值收口,非本队列事项)。按规矩 2 由**本轮闸门结果**将 UX-03 / UX-04 / UX-05 由 OPEN 改为 **CLOSED(jinyong-hud)**,三行证据列逐条点名场景与报告。视觉闸门本轮 blind(`endpoint_unreachable`,人眼代判)——不构成关闭障碍:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定;渲染墨迹可读性另由实测观测量兜底(`hp_text_width_ok` 墨宽、`HUD.nameplate_pairwise_overlap` / `hint_nameplate_overlap` 重叠钉),不冒充视觉裁决。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-27 `jinyong-hud`(终树复跑,本轮闸门实测):全部闸门在**含 no_energy 跟进实现的最终交付快照**上重跑--编译 **73/73** 零错误,playtest 硬闸门过、**50/50 场景全 PASS**:`skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5**、`spine_to_ending` **32/32**、`terminal_victory_8_12_rounds_hp_15_40` **6/6**;视觉闸门 blind(`endpoint_unreachable`,人眼代判),立场与上一条一致:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定,渲染墨迹由实测观测量(`hp_text_width_ok` 墨宽、nameplate / hint 重叠钉)兜底。上一条证据任务单引用的编译 72/72 是 no_energy 落地**之前**的运行;终树复跑全绿把最后缺的那节证据链接上——UX-03 / UX-04 / UX-05 的 **CLOSED(jinyong-hud)** 在最终交付树上成立,不再只押在落地前的运行上。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-27 `jinyong-clarity`:UX-06 / UX-07 / UX-08 的修复已落——属性页 `AttrDescLabel` 改为**常显全部五条属性效果**(`attr_effects_text()`,逐字来自 `_ATTR_DESCS` = `10_systems.md §1` + `40_progression.md §7`,零发明),属性页新增**当前气血数值行**(`HpValueLabel`,`hp_value = attrs["bone"] * 5`,`hp_text`),确认页新增**各属性最终数值列表**(`ConfirmSummaryLabel`,`confirm_summary_text` 五行「名 值」);`points_attrs_gap_ok` 的 CONFIRM 首行墨迹簇重指向 `ConfirmSummaryLabel`(同名同 yaml 断言行,jinyong-layout-r2 先例)。三个新场景 `creation_attr_effect_info` / `creation_hp_value_displayed` / `creation_confirm_summary`(50 → 53)。三条仍记 **OPEN**,按规矩 2:CLOSED 需闸门证据(`playtest_summary.md` 逐场景计数),而 post-fix 闸门证据待验——关闭决定由跑完回归闸门后的 5_design 证据步骤按实测单写(暂不写 CLOSED)。
- 2026-08-27 `jinyong-clarity`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **53/53 场景全 PASS**--`creation_attr_effect_info` **7/7**、`creation_hp_value_displayed` **10/10**、`creation_confirm_summary` **13/13**,既有回归网零红:`spine_to_ending` **32/32**、`creation_budget_clamp_and_traits` **11/11**、`creation_mouse_interaction` **14/14**、`creation_traits_back_next_buttons` **18/18**、`creation_back_to_menu_walk` **15/15**、`menu_to_creation_to_tutorial_order` **19/19**、`creation_single_ui` **16/16**、`creation_layout_readability` **22/22**(冻结几何两条钉未动);编译闸门 **74/74** 零错误。视觉闸门 blind(`endpoint_unreachable`,人眼代判)--不构成关闭障碍,立场与 jinyong-hud 关闭轮一致:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定,渲染墨迹由实测观测量(`points_attrs_gap_ok` / `creation_box_fits` / `desc_center_ok` / `nav_cluster_center_ok`)兜底,不冒充视觉裁决。按规矩 2 由**本轮闸门结果**将 UX-06 / UX-07 / UX-08 由 OPEN 改为 **CLOSED(jinyong-clarity)**,三行证据列逐条点名场景与计数。单元套件(18 文件含 `test_creation_info_texts.gd`)为结构事实,其 5_test 实测报告不在本步上下文,不作为关闭依据引用。至此队列无 OPEN 项(UX-01 为已拆分成 UX-01a/01b 的作废判据行)。
- 2026-08-27 `jinyong-spend-qi`(记录;不改任何 OPEN/CLOSED 状态,关闭由 5_design 证据步骤按闸门实测单写):UX-03 的 `cost_text` 现在显示**真实消耗**——8 个玩家招式 7 个有非零消耗(`内力 N`:大巧不工 15 / 力斩千钧 20 / 四海无量 25 / 心惊肉跳 10 / 拖泥带水 15 / 徘徊空谷 20 / 十七式 30),第 1 格重剑无锋按既有钉(`playtest/skill_button_effect_info.yaml` `SkillButton1.cost_text == "无消耗"`)与内容理由(免费基础招,玩家永不赤手空拳,见 `20_content.md §7.3`)保持「无消耗」。`no_energy` 状态由不可达变为可达,被新场景 `qi_cost_blocks_cast_no_energy` 在真实对局中钉住。
- 2026-08-27 `jinyong-spend-qi`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **54/54 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/54)——新场景 `qi_cost_blocks_cast_no_energy` **23/23**:真实对局里一次真实 skill_2 施放实测扣减内力(`energy < energy_max`,内力断言一律对上限相对表达、零绝对内力字面量),`debug_spend_player_qi` 排空内力池后第 4 格进入 `no_energy` 且**不是** `phase_locked`、disabled、标签「内力不足」,被拒的选中不消耗任何东西(无扣减、无冷却),第 1 格重剑无锋保持 ready 负对照。成本敏感判据 `terminal_victory_8_12_rounds_hp_15_40` **6/6**(`20_content.md` §7.2 预算 170/180 实测成立,消耗表无需回调)、`spine_to_ending` **32/32**;既有 53 条场景零红,其中 `skill_button_effect_info` **5/5**(第 1 格「无消耗」钉在真实消耗下原样保持绿)、`fahui_du_multiplies_damage` **10/10**、`central_divine_innate_qi_fatal_guard` **4/4**。编译闸门 **75/75** 零错误(新增 `tests/test_qi_costs_match_design.gd`,74 -> 75);视觉闸门 blind(`endpoint_unreachable`,人眼代判)——本轮**零新几何、零新调色板条目**(第 6 态与五对亮度分离在 jinyong-hud 已落地并被单测钉住),呈现层唯一改动是 EnergyLabel 文本逐帧跟随内力池,不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):UX-03 保持 **CLOSED(jinyong-hud)**,其关闭证据(`skill_button_effect_info` 5/5)在本轮新消耗下经实测复验仍然成立。上一条 jinyong-spend-qi 记录行所记「关闭由 5_design 证据步骤按闸门实测单写」以本行收口——无待关闭项,实测单如上。
- 2026-08-28 `jinyong-map-events`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **55/55 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/55)--新场景 `map_node_event_shaolin` **18/18**:进入少林即开事件(`phase == "EVENT"`、`event_id == "night_rain"` 确定性绑定钉、`current_node_id == "shaolin"`),两个选项都可聚焦选中(`event_focus` 0 -> 1 -> 0),回车解算后选项效果落地(`last_effect_types == ["silver", "attr"]` 结构钉 + `attr_bone: changed` 差分钉,数值断言一律相对表达、零绝对游戏值字面量)、`events_resolved_count` 阶梯 0 -> 1,已声明未实现缺口在节点上可断言(`entry_declared_gap_types` 含 battle 与 facility--诚实缺口是可断言事实),返程洛阳零事件零卡死;护线判据 `spine_to_ending` **32/32**(主线 5 节点槽位惰性在真实运行里成立,昆仑照常直达结局);既有 54 条场景零红,其中 `save_load_roundtrip` **14/14**(节点事件只在 travel 到达时触发、不落新持久键,存读档不复触发)与 `event_travel_effects` **19/19**、`cultivation_month_cycle_and_deck_bookkeeping` **17/17**、`cultivation_changes_combat` **30/30**(共享 `EventLogic` 后 cultivation 零回归、RNG 操作序未漂)。编译闸门 **77/77** 零错误(75 -> 77,新增 `scripts/data/event_logic.gd` 与 `tests/test_map_node_event.gd`);视觉闸门 blind(`endpoint_unreachable`,人眼代判)--本轮零新几何、零新调色板条目(事件面板走既有 `BodyLabel`,呈现层唯一改动是 EVENT 相位下隐藏旅行提示行,不留「回车启程」的过期承诺),不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):视觉闸门未产出新 finding、真人试玩未报新缺口,按规矩 3 不自动开卡;`20_content.md` §8 的同款实测确认由本行收口。
- 2026-08-29 `jinyong-nodes(主线事件)`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **57/57 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/57)--新场景 `map_node_event_mainline_east` **23/23**(直接 boot `map.tscn`,无名谷 -> 洛阳 -> 武当 -> 襄阳逐站钉 `merchant` / `quanzhen_scripture` / `dragon_scrap` 确定性绑定 + `events_resolved_count` 阶梯,收尾于昆仑一键之遥、`ended == false`)、`map_node_event_mainline_return` **20/20**(boot 帧零事件触发 + 返程 `tomb_bed` 触发并解算 + `HintLabel.text == "左右/上下选择相邻去处，回车启程"` 两处一文本钉 + `attr_inner: changed` 差分钉,数值断言一律相对);重排后的两条授权场景实测成立:`spine_to_ending` **42/42**(六段连着、走得到结局)、`map_node_event_shaolin` **32/32**(唯一重基线 `== 2` + 新增 `== 1` 阶梯钉);既有回归网零红:`save_load_roundtrip` **14/14**、`event_travel_effects` **19/19**、`cultivation_month_cycle_and_deck_bookkeeping` **17/17**、`cultivation_changes_combat` **30/30**、`terminal_victory_8_12_rounds_hp_15_40` **6/6**、`qi_cost_blocks_cast_no_energy` **23/23**。编译 **77/77** 零错误;视觉闸门 blind(`endpoint_unreachable`,人眼代判,57 场景 228 帧)--本轮零新几何、零新调色板条目(呈现层唯一改动是提示串逐字节换入既有 400px 居中 HintLabel),不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):地图页底部提示残缺(「左右选择」缺「上下」)是本轮 brief 点名的上一轮遗留,由本轮统一为面板同串并被新场景钉住,非本队列 finding;视觉闸门未产出新 finding、真人试玩未报新缺口,按规矩 3 不自动开卡;`20_content.md` §9 的同款实测确认由本行收口。
- 2026-08-28 `interaction-defects`(记录;不改任何 OPEN/CLOSED 状态,**status 保持 OPEN——fix landed in code/tests, gate evidence pending**,关闭由 5_design 证据步骤按闸门实测单写):本轮处理三条玩家实测 finding——(A) 悬浮血条 `Bar`(ProgressBar 默认 STOP)吃掉脚下右键、退回失灵,修复(Bar `mouse_filter=2` + 每帧重断言)已落,并补齐审计残项(`NameLabel` 显式 IGNORE、ClickTarget 实测裁定)与新场景 `click_move_undo_feet` / `input_click_differential`;(B) 立绘比所站格子高一整格、血条压腿、照立绘攻击打不到人——名牌改挂 `sprite_top` + `TileMarkers` 地面标记、5 步命中 resolver(第 2 步限 in-reach)已落,新场景 `click_portrait_body_targets_enemy` / `health_bar_above_portrait`;(C) 特质介绍要点了才显示——`trait_hover_index` 悬停预览(不写 `trait_index`、不触发 toggle、不影响焦点 modulate)已落,新场景 `trait_hover_preview`。
- 2026-08-28 `interaction-defects`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——硬闸门过(零 crash、零 runtime error)、编译 **83/83** 零错误,但 **15/65 场景断言失败(advisory,不翻硬闸门)**。按规矩 2 逐项裁定:**(A) 血条吃右键 → CLOSED(interaction-defects)**,证据 `playtest_summary.md`: `click_move_undo_feet` **7/7**(脚下 `Player +0,0` 右键退回起点;既有 `click_move_undo_right.yaml` 逐字节未动、仍 **10/10**)+ `input_click_differential` 左右键差分腿全过;**(B) 立绘/名牌/命中 → CLOSED(interaction-defects)**,证据:`click_portrait_body_targets_enemy` **9/9**(可达敌身体中心点击 → 掉血 + `acted == true`,out-of-reach 负对照不静默移动)、`health_bar_above_portrait` **12/12**(中排严格在立绘上方、顶行 Central_Divine/West_Poison 钳制落地、`tile_marker_count == 6`),七条验收网全绿(`click_move_to_tile` 10/10、`click_move_undo_right` 10/10、`click_move_commit_lock` 9/9、`click_targeting_fixed` 2/2、`move_target_affordance` 18/18、`health_bar_numbers` 5/5、`portrait_visibility` 22/22),触屏退回 `undo_button_retreat` **18/18**;**(C) 特质悬停 → CLOSED(interaction-defects)**,证据:`trait_hover_preview` **21/21**(悬停即显、`trait_index` 不动、移开回退)。视觉闸门 blind(`endpoint_unreachable`,人眼代判放行)——与既往关闭轮同立场:以上关闭依据全是 playtest 数字断言,不以盲视觉裁决为证据。上行所记审计残项两处以闸门实测勘误:`NameLabel` 显式 IGNORE **未落**(交付 tscn 无该行,靠 Label 类默认 IGNORE,子树无 STOP 的结论不变)、ClickTarget「实测裁定」**未发生**(见下)。**未关项(如实;一条诚实的 OPEN 优于无证据的 CLOSED):**(1) `input_click_differential` **12/13**——`debug_click_target_fires` 计数器未落进 `enemy.gd`(`_common.yaml` 白名单与场景断言先行、实现缺席,断言报 `Invalid named index`),ClickTarget 实测钉子待落(勘误详见 `30_presentation.md`);(2) `map_hint_single` **6/7**——EVENT 相「上下选择，回车定夺」钉观测到英文面板串(语言缺陷下游),TRAVEL 半边(页脚带提示、面板不重复)两条钉已过;(3) `ui_geometry_readability` **35/38**——`follow_delta 30.0 > 24`(顶行钳制位移 94−64=30,与 `bar_top == 94` 钉对顶行互斥;B1 声明的 re-baseline 未落)与 `hint_nameplate_overlap == true`(B1 抬锚 + UndoButton 轮 `SkillDescLabel` 下移的叠加,推算与处置见 `30_presentation.md` 名牌节);(4) 其余 12 条红同根:**无头闸门下 UI 以英文渲染**——`settings_manager.gd::_detect_language` 旧守卫键在 `DisplayServer == "headless"`,而闸门为取帧跑 RENDER 模式(Xvfb),守卫从不触发 → `language=en`(代码注释已实测点名 `Points left: 30`、`Up/down to choose, Enter to decide`);修复已落(仅 web 出口自动检测,其余一律 zh),**post-fix 闸门证据待验**——预期 `round_one_snapshot_and_turn_order`(12/14)、`fahui_du_multiplies_damage`(2/10)、`cultivation_changes_combat`(27/30)、`skill_rejection_reason_texts`(2/3)、`skill_button_effect_info`(3/5)、`settings_panel`(11/14)、`creation_single_ui`(15/16)、`creation_hp_value_displayed`(7/10)、`creation_attr_effect_info`(5/7)、`creation_confirm_summary`(12/13)、`creation_traits_back_next_buttons`(17/18)、`creation_layout_readability`(21/22,疑为英文墨宽下游)零场景改动转绿,届时由下一轮证据单收口。本轮任务清单未触碰语言层;上一个全绿整树实测是 jinyong-nodes 的 57/57,其后未再整树实测,回归落在其间的未实测改动里。
- 2026-08-28 `interaction-defects`(修复轮):本轮五处修复已落(ClickTarget 计数器 + `input_click_differential` 钉、语言默认守卫 + 运行时 `zh` 钉、几何翻转 + `SkillDescLabel` 让位、静态守卫、文档/记录对齐)。涉及的真实输入/STOP-filter 覆盖与名牌可读性两条 finding **status 保持 OPEN——修复已落,修复后闸门证据待跑**;关闭判定留给 post-gate 5_design 证据步骤按闸门实测单写,不在此前置关闭。
- 2026-08-28 `camera-owns-visibility`(记录;**不改任何 OPEN/CLOSED 状态,status 按规矩 2 留给 5_design 闸门后证据单**):本轮拆掉 `GridManager.clamp_sprite_offset` 及其逼出的补偿,由跟随 Camera2D 拥有可见性(见 `30_presentation.md` 定位章 / `90_decisions.md` 2026-08-28 条)。**本轮回档 UX-01b**:它前一次的 **CLOSED(jinyong-events)是引入钳位买来的**——`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92` 把立绘向下推出自家格,骗过 `portrait_covered_frac < 0.25`;本轮删除钳位、新增对位 pin 让「立绘站在自己格子上」成为可断言事实,才算真正修好。按规矩 2,UX-01b 的状态翻转由跑完回归闸门后的 5_design 证据步骤按闸门实测单写,此处只回档 + 证据路径。证据路径(仓库内真实路径):`scripts/camera_follower.gd`、`scripts/coord.gd`、`scripts/autoload/grid_manager.gd`(`board_rect()`)、`playtest/portrait_grid_alignment.yaml`、`playtest/camera_transform_follows_unit.yaml`、`playtest/portrait_visibility.yaml`(已改写为相机级)。同时记录两条新 finding:UX-09(小地图 / 屏外单位边缘指示,整盘看不全成常态后升优先级)与 UX-10(立绘:格子尺寸比 96/64 现为可调内容决策,是 TileMarkers 唯一残留理由)。
- 2026-08-28 `camera-owns-visibility`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——硬闸门过(零 crash、零 runtime error)、编译 **85/85** 零错误,**68 场景 65 绿 3 红(advisory,不翻硬闸门)**。按规矩 2 逐项裁定:**(1) UX-01b 重关为 CLOSED(camera-owns-visibility)**:证据 = 改写为相机级闸门后的 `portrait_visibility` **9/9** + 变换钉 `camera_transform_follows_unit` **9/9** + 对位 pin `portrait_grid_alignment` **27/29 中的 24 条墨迹对位线全绿**(f40 静态腿与走位帧复断各 12 条,含相机钳在 cam_y_lo 的北带帧)——「立绘站在自家格上」由绿线证明;红的两条是走位腿自证(`grid_pos == Vector2i(9,1)` 实测 (6,2)、`moves_left == 2` 实测 4),即 yaml 行内预言的 **red-for-the-wrong-reason**:腿没走到预订格,处置照其行内指引=下一轮报告五敌 grid_pos 并重排走位腿,**不动 ink 阈值**。行 1 单位头顶约 32px 被顶栏遮住是显式接受的取景代价(推导见 `90_decisions.md` 2026-08-28 条),不再由精灵级判据裁定。**(2) 冻结验收网 3 绿 1 红,红因已查明、非相机回归**:`click_move_undo_right` **10/10**、`move_target_affordance` **18/18**、`click_move_to_tile` **10/10**;`click_move_commit_lock` **4/9**——其 yaml 仍按上一轮 fix_toprow_spawn_positions 的几何编写(Central_Divine (7,3)、合法行 3..10),本轮恢复出生点 (7,1)/行 1..9 后未随之重推导:f45/f100 证明移动腿正常(到 (7,4)、余 3),f105 对 `Central_Divine_ClickTarget` 的攻击点击因 Chebyshev 距离 3 不可达被五步解析器**正确拒绝**(acted 恒 false、health 未动),f155 的右键于是按规则**合法撤销**了移动(f185 实测 (7,5)、余 4)。修法=按恢复后的几何重推导该 yaml(走到 (7,1) 的邻格再攻击、再验 undo 拒绝),零断言放松。**(3) `input_click_differential` 12/13**:`Central_Divine.debug_click_target_fires == 0` 实测 **1**——brief 保留项 `scenes/enemy.tscn` ClickTarget `mouse_filter=2` 未落(仍 0/STOP),计数器实测该 STOP 控件吃掉按下,与 brief 记录的实测 == 1 一致;修法=落一行 tscn 改动、保持 `== 0` 腿。其余 65 条场景全绿,含 `spine_to_ending` **42/42**。UX-09 / UX-10 保持 OPEN(本轮记不实现)。

## 人工视觉判读的发现(2026-08-28,轮次所有者)

`5_vision` 这一轮是**瞎的**:视觉判官 `localqwen/qwen3`
(`http://100.68.74.107:8000`)连接被拒,260 帧一帧未判。闸门**正确地拒绝通过**
(`blind: true, passed: false`,不是「没跑到所以算过」),`5_vision_judged` 于是转人工。

人工判读是**抽样**,不是复审:看了 260 帧中的 4 帧(s11 战斗、s16 属性页与特质页)。
结论是**本轮未引入视觉回归**,故放行;以下是抽样中看到的**既有欠账**,与本轮 brief
(交互缺陷 B/C)无关,记在这里等立项。

### 战斗屏:好

山水背景、六名角色 96×128 立绘、名牌与血条浮于头顶、招式栏满格。
**「美术仍是色块」这句已彻底不成立**,`00_roadmap.md` 第 4 阶段的描述已相应更新。

### 创建角色屏:两页都差,而这是新玩家看到的第一屏

1. **属性页的 ± 按钮竖直方向不成列。** 每行作为整体居中,而标签宽度不同
   (`Bone 20` / `Agility 10` / `Fortune 10`),按钮因此左右参差——实测 `-` 的 x 在
   470~487 之间跳。行与行之间没有对齐轴,眼睛要重新找一次。
2. **特质页 12 条只有名字与数字,没有任何介绍区域。** 这是玩家报告的缺陷 C 的另一面:
   本轮做了 `trait_hover_preview`,但抽到的帧里没有描述面板出现,需要在下一轮用一帧
   悬停态确认它真的会显示(本轮的 `creation_hover_scenario` 断言了行为,没有断言可见)。
3. **两页都是居中细长列,左右各空约三分之一、下方空约四分之一。** 960×704 的画布用了
   不到一半。
4. 属性页的说明是一段挤在一起的长句(`Bone:HP = Bone × 5 · Qi:Qi pool = ...`),
   没有分行分组。

### 一条与语言有关的观察

抽样帧里满屏英文中夹着一个中文「退回」按钮(战斗屏右侧)。i18n 有覆盖缺口——
该帧拍摄于语言修复之前,但**未翻译的串会以这种方式混排**这一点与语言设置无关,
下一轮值得做一次覆盖率清点。

