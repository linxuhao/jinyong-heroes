# Progression — 第 2~6 段与全局公式

> **本文件是「已决定、未实现」层。** `10_`/`20_`/`30_` 只写**已经跑起来**的
> 东西(README Rule 3);`90_decisions.md` 写**还没决定**的。本文件夹在中间:
> 决定已经作出、可以照着实现,但代码里还没有。每落地一段,把该段的规则
> **搬进** `10_`/`20_`/`30_`,并从这里删掉——这个文件应该越来越薄,
> 空了就删。

`00_overview.md` 定了六段的**顺序与立意**,本文件定它们的**内容**。
在此之前这些问题都躺在 `90_decisions.md` 的 Open questions 里,标着"待决";
没有答案,框架就搭不起来——不是缺代码,是缺决定。

## 0. 贯穿原则:先把整条线走通,再加深

**每一段都可以薄,但不能不存在。** 从教程战打完到看见结局,玩家必须能
一路走到底,哪怕中间某段只有三个按钮。

理由是上一轮的教训:六段里只有第 1 段存在,于是所有精力都花在给那一场仗
调伤害小数点,而"穿越之后是什么"一行代码都没有。一条能走通的细线能告诉你
哪一段真的不好玩;一段打磨到发亮的教程告诉不了你任何事。

推论:本阶段**不做**美术、音效、动画打磨;每段的验收标准是"走得过去且
状态正确",不是"好玩"。

## 1. 第 2 段 · 穿越

**形式:文本过场**(全屏文字 + 一张背景图 + 继续键),不是过场动画,
也不是必败战。

- 必败战被否决:`00_overview.md` 说教程"必须能赢,而且赢得漂亮——它是被夺走的
  那个东西"。紧接着安排一场必败,是把刚给的东西当场作废两次。
- 主角是**又一个平行版本**,不是原创角色。穿越后**修为归零、见识留下**:
  玩家(不是角色)记得那八个招式怎么用,而角色一身修为没了。这条也解释了
  为什么第 3 段一个新人能听懂武理。

**状态后果:** 清空 `PlayerProfile` 的功法与属性,保留一条"教程已完成"的标记。

## 2. 第 3 段 · 开局选择

**五个门派。** 选一个入门,给**一门丁级内功 + 一门丁级外功**作为起手。

| 门派 | 内功 | 外功(门类) |
|---|---|---|
| 少林 | 易筋经·入门(丁·刚) | 罗汉拳(丁,**拳掌**) |
| 武当 | 纯阳无极功·入门(丁·柔) | 太极剑(丁,**剑**) |
| 丐帮 | 混天功·入门(丁·阳) | 打狗棒法(丁,**长兵**) |
| 峨眉 | 峨眉九阳功·入门(丁·阴) | 峨眉剑法(丁,**剑**) |
| 唐门 | 唐门心法·入门(丁·柔) | 满天花雨(丁,**暗器**) |

五个门派覆盖 10 门类里的 4 类(拳掌 / 剑 / 长兵 / 暗器),够第 4 段起步;
其余门类留给游历与奇遇。

**"现代版的金庸群侠传"指系统表达现代化**——数值透明、反馈明确、
不靠玩家查攻略——**不是**现代都市外壳。世界仍是古代江湖。

## 3. 第 4 段 · 养成

**限时日程:固定 48 周。** 走完进入第 6 段。

**一周一个时段**(不是侠客风云传的三时段)。这是本阶段的简化:先让 48 次
决策跑通,时段细分随时可以加,加了不会推翻任何结构。

每周四选一:

| 行动 | 效果 |
|---|---|
| **练功** | 选一门已学功法,+经验;满级则该门类前置 +1 |
| **修习** | 选一项属性,+1~3(查表,不用曲线) |
| **做工** | +银两 |
| **游历** | 触发一个第 5 段事件 |

**养成必须改变战斗里看得见的东西**,否则这一段是空转。改变的通道就是
已经设计好的**发挥度**(`10_systems.md` §4):练满乙丙丁 → 甲级功法的
前置齐了 → 发挥度从 0.6 爬到 1.0 以上 → 伤害数字当场变大。

所以本段的硬要求是:**`gongfa_data.gd:get_fa_hui_du()` 的写死 1.3 必须换成
真算**。目前那个 stub 自己的注释就写着 "the 甲乙丙丁 cascade is NOT
implemented" —— 不实现它,养成推 48 周和推 0 周没有任何区别。

**首批填满阶梯的门类:剑、拳掌、轻功。**(`90_decisions.md` 原本的倾向,
采纳:覆盖面广、招式好设计,且与五个门派的起手外功对得上三门。)

## 4. 第 5 段 · 活动与战斗

**文本选项事件 + 遭遇战。** 事件是一段文字 + 2~3 个选项,选项后果作用在
属性 / 银两 / 功法经验 / 触发一场战斗上。

**好感度不做**(移入 `90_decisions.md` 的 Out of scope)。它需要一整套角色
关系与好感衰减,而本作的角色是"同名平行变体"——谁对谁的好感本身就没有
连续性可言。

遭遇战复用教程战的战斗场景与全部战斗系统,只换参战单位与地形。

## 5. 第 6 段 · 大地图

**节点移动。** 一张江湖图上若干地点,点击相邻节点移动,节点上触发战斗、
事件或门派设施。不做格子移动(那是战斗的词汇),也不做自由移动(要碰撞、
要寻路,买不到相应的玩法)。

**有主线,极简:查明自己为何穿越。** 若干节点串成一条线,走完即**结局**。
结局按最终属性 / 功法成就分级——这是"游戏结局"这四个字在本阶段的兑现。

**门派势力关系不做**(移入 Out of scope):它要求门派之间有可演化的状态,
而当前门派只是开局分支。

## 6. 全局 · 属性派生公式

| 派生 | 公式 |
|---|---|
| 气血 | 根骨 × 5 |
| 内力值 | 内力 × 2 |
| 移动力 | 2 + 身法 / 30(向下取整) |
| 先攻 | 身法 |

**教程战的满级杨过与五绝不走这套公式**,继续用 `20_content.md` 里直接给定的
数值。教程是一段编排好的展示,它的数值是被平衡过的(8~12 回合、剩余
75~200);套公式会把刚调好的仗打散(根骨 92 × 5 = 460 ≠ 500)。

这不是例外主义,是分工:**公式服务于养成出来的角色**,教程战服务于开场体验。
代码里两条路径都要有,且教程那条要显式标注为"编排数值",不许被后续 run
当成漏改的旧值"修正"过去。

## 7. 全局 · 存档

`user://save_<slot>.json`,**3 个存档位**,JSON 明文(可读、可 diff、
出问题看得见)。

内容:`PlayerProfile`(属性 / 已学功法及等级 / 银两 / 背包)、当前段落、
养成周数、已触发事件、大地图位置。

## 8. 全局 · 招式数量

**技能栏 8 格,即同时装备 2 门外功**(甲级外功给 4 招)。已学功法不限数量,
**战前选装**。8 格是教程战已经验证过的操作上限,不再上调。

## 9. 乐器门类的一般规则

音功偏**大范围、低伤害、带增益/减益**(碧海潮生:全场 18 + 全体先攻 −20)。
本阶段不产出新的乐器内容,规则方向先记在这里,免得后续 run 各自发明。
