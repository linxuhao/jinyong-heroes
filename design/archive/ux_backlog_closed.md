# 40_ux_backlog.md — CLOSED 项归档 (moved verbatim from design/40_ux_backlog.md)

> Source: `design/40_ux_backlog.md` (2026-09-03 ledger slimming, Card N+2 / C7.2).
> The CLOSED queue rows, the full `## 记录` log, and the blind-judge manual frame-read
> prose below were moved **verbatim** (byte-identical to the source at move time) so the
> current file keeps only OPEN items. The current `design/40_ux_backlog.md` carries one
> pointer row per closed id. The voided-criterion row UX-01 (`~~WONTFIX~~`) is archived
> here as part of the closed-history block (it must not be deleted outright).

## 队列 — CLOSED 项

| id | 状态 | 界面 | 看见什么 | 玩家因此做不到什么 |
|---|---|---|---|---|
| UX-01a | **CLOSED(jinyong-events)** — 实测无缺陷 | 战斗棋盘 | **实测:杨过立绘在帧上**。`final/portrait_cover_probe_notes.md` f40:`Player.portrait_visible=true`、`fail_layer=""`,八层判据全过,`covered_frac=0.104166666666667` 亚阈值;三数探针自洽(`sprite_pos [480,352]` + `tex_size [96,128]` + `bar_pos [446,320]` → 墨迹 [432,528]×[224,352] 棋盘中部)。早先「那一格什么都没画」是人读帧误判 → **frame-reading divergence,no fix** | 原 finding「看不出主角站哪格、点不到他」经实测不成立——立绘确在帧上,棋盘中部 |
| UX-01b | **CLOSED(camera-owns-visibility)**(2026-08-28 重关;前次 CLOSED(jinyong-events) 是引入钳位买来的,本轮拆钳位真修) | 战斗棋盘 | **实测 f40:王重阳立绘被顶栏部分遮挡(修前 RED)**。`final/portrait_cover_probe_notes.md`:`Central_Divine.portrait_visible=false`、`fail_layer="covered"`、`covered_frac=0.333333333333333`(≥0.25 阈值、≥64px² 绝对下限)、`sprite_top=0.0`。八层判据的 `covered` 层首次抓到部分遮挡(旧 `occluded` 只认完全包住)。修复已落:`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92`。**post-fix 闸门证据已上盘:playtest_summary.md `portrait_visibility` 22/22 全绿、`Central_Divine.portrait_covered_frac < 0.25` → 按规矩 2 由闸门证据声明 CLOSED**(该次关闭由引入 `clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92` 买来——立绘被向下推出自家格骗过 `covered_frac < 0.25`);**2026-08-28 重关为 CLOSED(camera-owns-visibility)**:本轮拆钳位、可见性归跟随相机。闸门实测(本轮 `5_compile` 的 `playtest_summary.md`):改写为相机级闸门的 `playtest/portrait_visibility.yaml` **9/9**(`follow_target_is_active == true`、`active_unit_screen_y` ∈ `[hud_band_top, hud_band_bottom]`、`camera_position.y` ∈ `[camera_y_lo, camera_y_hi]`)、变换钉 `playtest/camera_transform_follows_unit.yaml` **9/9**、对位 pin `playtest/portrait_grid_alignment.yaml` **30/30 全绿**(2026-08-29 re-loop 闸门复跑:f40 静态腿与走位腿到达帧 f820 各 12 条墨迹对位线全绿,`grid_pos == Vector2i(6,1)` / `moves_left == 3` 自证同行;dx/dy≈0,立绘站在自家格上——走位腿重排过程见记录行)。行 1 单位头顶约 32px 被顶栏遮住是显式接受的取景代价(`90_decisions.md` 2026-08-28 条) | 认不出那是谁——王重阳立绘顶部约 1/3 被顶栏不透明面板盖住 |
| UX-01 | ~~WONTFIX~~ **判据太弱,该结论作废** | — | 原判据 `portrait_visible == true` 六个单位全绿,而人眼在同一帧上看到两个单位有问题。`portrait_probe_notes.md` 至今仍写着 PENDING/(not run),那份 WONTFIX 引用的「实测」实际来自闸门的 `portrait_visibility` 10/10 —— 而那正是这条太弱的判据 | **教训:一个断言全绿不等于缺陷不在;先看真帧** |
| UX-02 | **CLOSED(jinyong-affordance)** | 战斗棋盘 | 移动落点只有一个黄色空框,**没有任何确认 / 取消的可见提示** | 不知道可以右键退回 —— 右键退回这一轮做完了,可供性一个字都没有。**证据:final/move_hint_probe_notes.md、playtest/move_target_affordance.yaml(状态跟随中文提示,mouse_filter = 2,2026-08-25 闸门实测 18/18)** |
| UX-03 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 底部技能栏 | 技能按钮只有名称和「发挥 ×1.3」,没有效果说明,也没有内力消耗 | 无法判断该用哪一招,也不知道放完还剩多少内力。**修复已落并经本轮闸门实测**:`effect_text`(招式中文描述,悬停 tooltip 同文)+ `effect_summary_text`(≤ 6 字面效果摘要,数值只取自既有 `SkillData`,不发明数字)+ `cost_text`(「无消耗」——`SkillData.cost` 默认 0,消耗缺口如实记入 `20_content.md §5`);内力余量由既有 `EnergyLabel`「内力: 180」承担。**证据:playtest_summary.md `skill_button_effect_info` 5/5 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**;修前 A 类红(观测量整组缺席)见 `final/hud_info_probe_notes.md` §1 |
| UX-04 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 底部技能栏 5–8 格 | 只写「锁定」,没有锁定原因或解锁条件 | 不知道为什么不能用,也不知道怎么解锁。**修复已落并经本轮闸门实测**:`lock_reason_text`「第 4 轮解锁」由 HUD 每帧派生自**既有** tutorial phase-lock 谓词(`tutorial_battle and i >= 4 and current_round < 4`),遭遇战 / 第 4 轮起为空串,绝不写死常显字符串。**证据:playtest_summary.md `locked_slot_unlock_reason` 8/8 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**——含 5–8 格 reason 非空、第 1 格为空、第 4 轮起 reason 归空且 `state_text != phase_locked`;修前 A 类红见 `final/hud_info_probe_notes.md` §1 |
| UX-05 | **CLOSED(jinyong-hud)** - 闸门证据已落 | 血条 | 条上没有任何数字或刻度 | 只能看出大概,说不出具体还剩多少血。**注**:2026-08-25 已把「有填充/有空白」修到原始尺寸下可辨(见 99_changelog)。**修复已落并经本轮闸门实测**:`HpLabel`(`Bar` 子节点,画在填充与 EmptyCap 之上)显示**当前值**(post-review 返工 `fix_hp_number_readability_v2`:64×12 的条装不下 9 个字的「1000/1000」,cur/max 路线弃用,见 `30_presentation.md`);`hp_text` / `hp_value` / `hp_max` 观测量 + `hp_text_width_ok` 实测墨宽,断言一律对 `max_health` 表达、零绝对血量字面量;health_bar 三件套几何常数逐字节未动。**证据:playtest_summary.md `health_bar_numbers` 5/5 全绿(2026-08-26 本轮闸门实测,50/50 场景全绿)**;修前 A 类红见 `final/hud_info_probe_notes.md` §1 |
| UX-06 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 属性页 | 内力 / 身法 / 悟性 / 福缘只有名称和数值,没有效果说明 | 不知道这些属性有什么用,无法决定把点加到哪一项。**修复已落并经本轮闸门实测**:`AttrDescLabel` 从「焦点属性的效果」改为**静止即全列五条属性效果**(`attr_effects_text()`,名称前缀,逐字复用 `creation.gd::_ATTR_DESCS` = `10_systems.md §1` 释义 + `40_progression.md §7` 公式,零发明、无内容缺口见 `20_content.md §6`;`attr_index` 仍只驱动行高亮与 +/- 目标),场景含 move_down 焦点循环腿证明列表**不随焦点消失**。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_attr_effect_info` 7/7 全绿(53/53 场景全 PASS;编译 74/74 零错误)**;修前 A 类红(静止时仅焦点属性的效果出现)见 `final/creation_info_probe_notes.md` §4 |
| UX-07 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 属性页 | 只显示公式「气血 = 根骨 × 5」,不显示当前气血数值 | 要自己心算才知道现在多少气血。**修复已落并经本轮闸门实测**:效果列表正下方新增 `HpValueLabel`(加法节点,冻结几何零改动)显示**当前气血**(格式 `当前气血 %d`,数值 `hp_value = hp_from_bone(attrs["bone"])` = 根骨 × 5,§7 公式的活值,随每次加点在 `_render()` 实时刷新);断言一律相对表达(`hp_value == attrs["bone"] * 5`),**零绝对血量字面量**(health_bar_numbers 同款纪律)。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_hp_value_displayed` 10/10 全绿(53/53 场景全 PASS;含 move_right 后 `hp_value: changed`、再回等的跟踪腿)**;修前 A 类红(节点整组缺席)见 `final/creation_info_probe_notes.md` §1 |
| UX-08 | **CLOSED(jinyong-clarity)** - 闸门证据已落 | 捏人 · 确认页 | 只有「剩余点数」和两个按钮,不列出各属性最终数值 | 点「确认踏上江湖」之前没法核对自己捏了什么。**修复已落并经本轮闸门实测**:`ConfirmBox` 首子新增 `ConfirmSummaryLabel`,每属性一行「名 值」(`confirm_summary_text_from(attrs)`,五行),位于两个按钮之上、仅 CONFIRM 可见(BackButton 返回 TRAITS 即隐藏,场景钉住);逐属性断言相对表达(`contains("根骨 " + str(attrs["bone"]))`);CONFIRM 几何钉(`points_attrs_gap_ok`(重指首行墨迹簇)/ `phase_skeleton_same` / `creation_box_fits` / `nav_cluster_center_ok`)证明摘要进入冻结布局而**未挪动任何常数**。**证据:本轮 `5_compile` 的 `playtest_summary.md` 实测 `creation_confirm_summary` 13/13 全绿(53/53 场景全 PASS;含 `creation_layout_readability` 22/22 零回归,即重指后既有 yaml 断言行原样保持绿)**;修前 A 类红(节点缺席)见 `final/creation_info_probe_notes.md` §2 |
| UX-13 | **CLOSED(jinyong-equipment-battle)** — 闸门证据:`playtest_summary.md: roster_equip_free_action 36/36 PASS`(2026-08-31 本轮官方运行:真实 merchant 事件发牌 → 点击「装上」→ `equipped_weapon == "eq_sword_3"` → 点击「卸下」→ 回空槽 → 关面板,month / phase / 计数全不变,`cursor_markers_visible == false` 保持)。修复:`PlayerProfile.equipped` 三槽(全 String 键 JSON 无损,读档修复 `equipped ⊆ inventory`,老存档 = 三空槽不崩不清)+ 角色页装 / 卸按钮池(focus_mode 0、自由动作、无 autosave)+ 单一 tier 公式(`40_progression.md` §9.1)。**范围注:装备「进战斗」的差分闸门不在本条关闭范围**——`equipment_in_battle_diff` 首轮官方运行 16/32 红,post-fix 官方复跑 **47/47**,UX-16 据此 **CLOSED(jinyong-equipment-battle)**(2026-08-31) | 玩家看得见抽到的装备,却装不上——「装上」已还账(战斗差分证据另记 UX-16) |
| UX-16 | **CLOSED(jinyong-equipment-battle)** — post-fix 闸门证据已落:本轮官方闸门 `playtest_summary.md`(2026-08-31,77 场景全 PASS)实测 `equipment_in_battle_diff` **47/47**、硬闸门 `passed: true`、零 runtime error | 角色页/装备/战斗 | 首轮官方运行 16/32 红(6 条 runtime error:`aim: node not found: RosterOpenButton / EquipButton0 / RosterCloseButton`,断言首红 f630 `GameManager.current_state == "MAP"` 实测 `BATTLE`)——根因不在帧布局而在路由:MAP(华山)战斗复用教程战场(`battle_return_state != "CULTIVATION"` 分支),从不调 `battlefield.gd:651 BattleSetup.build_character(SaveManager.profile)`,任何帧重排都到不了差分;修法 = 场景重写为**真实养成遭遇战路径**(`debug_enter_encounter` → `build_character(profile)` → `derive_stats` → `EquipmentData.sum_bonuses`),物品经白名单 `debug_grant_equip` → `EventLogic.apply_option_effects` 入 inventory(绝非裸写 profile),每条战斗腿断 `CombatManager.tutorial_battle == false`。修红实测四值(临时回退法直连边车,逐字节还原):失败帧 **f560** / 首断 **`Player.gear_attack_bonus: gear_attack_bonus > 0`** / 确切错误 **`FAIL f560 Player.gear_attack_bonus: gear_attack_bonus > 0 (observed=0)`** / 红前绿 **46**;还原后 post-fix 官方复跑 **47/47**(未装备 `gear_*` 全 0 → 装上 `gear_attack_bonus` changed 且 > 0 → 卸下回 0,可逆) | 「换把剑,这场战斗数值就该不一样」的官方证明已到手——由 `equipment_in_battle_diff` 47/47 的 changed 差分承载(零断言放松、零阈值放宽、零冻结场景改动) |
| UX-17 | **CLOSED(jinyong-shrimpcopy2)** — post-fix 闸门证据已落:本轮官方闸门 `playtest_summary.md`(2026-08-31,**78/78 场景全 PASS**、硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error)实测 `event_pool_new_event_resolved` **15/15**——重写后的事件散文在屏上走完「抽到 / 渲染 / 选中 / 结算」四腿,场景两枚中文字面量钉 `崖上采药` / `重金购芝` 逐字节匹配交付数据(零 diff);既有网零回归(`event_travel_effects` **19/19**、`spine_to_ending` **42/42**);编译 **95/95** 零错误。散文内容断言(28 条 A 类重写 / 8 条 B 类逐字未动;守卫三张 denylist 经独立直读复核对交付文件零命中)由本轮验证报告(`final/verify_report.json`)承载;pytest 守卫与 GDScript 单元套件(EN 成员资格闸、`_test_no_repeat_full_journey`)的官方 PASS 仍以 5_test 产物(`test_report.json`)为准——不在本步上下文,不冒充实测 | 游历事件文案 | 所有者 2026-08-31 裁定:**一切角色都是虾,路人也不例外**(2026-08-28 裁定的落地答案)。本轮把 36 条事件散文全部改归虾体——28 条 A 类行重写(34 个散文字段 = 5 标题 山道遇劫匪→山道遇劫 / 行商路过→车马过路 / 老丐乞食→巷口乞食 / 醉汉传拳→酒肆拳影 / 坠马客商→途中坠马 + 27 正文 + 2 标签 卖与书贾→卖与书铺 / 送还失主→归还失物),劫匪/行商/老丐/掌柜/郎中/村民/少年/艄公/镖头/老僧/老道/药翁/说书人/剑客/刀主/向导/巫师/老铁匠与 手提/伸手/拍着胸脯/手舞足蹈/揉着腰 等人体动作全部以 钳/螯/须/甲壳/蜷起的尾节 行文;8 条 B 类行(ruins/tomb_bed/wounded_eagle/peach_maze/ancient_bell/wedding_train/sword_mound/wild_goose_letter)逐字未动;路人一律不点虾种(六种已定虾种仍归六位命名角色),河伯保持名号,巨雕/猴子/大雁/蛇/虎/马仍是动物;客栈/镖局/山道/渡口/马车/银两/钢刀等江湖器物逐字保留、零海底化,flood_ferry「泅水而过」按陆上武功保留并受守卫保护。新守卫 `tests/test_event_prose_shrimp.py`(stdlib-only):HUMAN ×38 / UNDERWATER ×12 / SPECIES ×13 denylist + 4 处受保护字面量,先落先红(改前语料 39 对 (行,token),`final/shrimp_guard_red_first_notes.md`),交付文件上零命中 | 「所有人物都是虾」在玩家读得最多的 36 条事件里成立;「冻结 16 条」仅散文半边解冻(裁定与理由见 `90_decisions.md` 2026-08-31 jinyong-shrimpcopy2 条),id / effects / 选项结构 / 36 行数逐字节未动(`ROW_EFFECTS` 镜像未改即机器证明) |
| UX-18 | **CLOSED(jinyong-event-pool-36)** — post-fix 闸门证据已落:本轮官方闸门 `playtest_summary.md`(2026-08-31,**78 场景全 PASS**、硬闸门 `passed: true`、零 runtime error)实测 `event_pool_new_event_resolved` **15/15** | 游历事件观测面(本轮新增) | 首轮官方 playtest 闸门 `playtest_summary.md` 实测 `event_pool_new_event_resolved` **13/15**——f200 两断言红:`CultivationScreen.event_title == "崖上采药"` 实测**空串**、`CultivationScreen.event_body != ""` 实测**空串**;同帧及其余断言全绿:`event_id == "cliff_herbs"` 抽中、`events_seen_count == 35`(f140/f200)、f210 `focused_option_text` 选中、f230 `events_seen_count == 36` 结算阶梯 + 相位/月份回 CARD_PICK——「抽到 / 选中 / 结算 / 无清空」屏上实测成立,唯「渲染」腿缺 | 缺陷落点在**本轮新增的两个观测面**(`cultivation.gd` `_sync_surface` 发布 `event_title`/`event_body`)的时序/接线,不在玩法——既有事件渲染走 `tr()` 标签路径(冻结 16 条 `event_travel_effects` **19/19** 仍绿),视觉闸门 Q6 78 好 / 0 坏;修法已落并经 post-fix 官方复跑实测收口:`cultivation.gd` ACTION_PICK case 3 抽取后立即 `_sync_surface()` 发布(裁定 `90_decisions.md` (g))→ `event_pool_new_event_resolved` **15/15**、78/78 全 PASS,零断言放松、零阈值放宽、零冻结场景改动 |
| UX-22 | **CLOSED(jinyong-theme)** — 闸门证据已落:本轮官方闸门 `playtest_summary.md`(2026-09-01,硬闸门 passed、零 runtime error)实测 `roster_panel_item_nail` **36/36**、`roster_panel_cultivation_open_close` **16/16**;视觉闸门 passed 非盲(79 场景 316 帧,六问全 `failed: false`,Q6 **78 好 / 1 坏**——唯一坏答是 `tutorial_loss_restarts_tutorial` 的战斗帧,非角色面板;含角色面板帧的各场景 Q6 均答文字全可读) | 角色页(RosterPanel) | 角色页面板透明,背景屏文字透出串字,读面板与读背景互相干扰 | **修复已落并经闸门实测**:裸 `RosterBox` / 教程 `Panel` 由全局主题 `Panel/styles/panel` **不透明**墨底板兜底(信息层第一次有真底),`roster_panel.tscn` RosterDim 0.55→0.85 —— 结构性修复,非 alpha 微调;零 z-order / 层级 / 坐标改动 |
| UX-31 | **CLOSED(jinyong-theme)** — post-fix 闸门证据已落:本轮官方 `playtest_summary.md`(2026-09-01,**79/79 场景全 PASS**、硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error)实测 `creation_layout_readability` **22/22**(f90 `creation_box_fits` 转绿);修红实测 21/22 FAIL(f90 observed=false,红前绿 21)→ 同帧对照 22/22(`final/delivery_notes_theme.md` §7) | 捏人屏 | 主题把全局 Label 字号 12→14、Button 15 后,捏人屏固定框装不下了:pre-fix 官方 playtest `creation_layout_readability` **21/22**,唯一红 f90 `CreationScreen.creation_box_fits` 实测 `False`(该场景此前每轮全绿,系本轮主题字号增长引入的回归;五条保护闸门不在其列,但「既有场景零回归」被本轮打破,如实入列) | **修复已落并经官方 post-fix 复跑实测收口**:D6 预写回退路按落执行——`scenes/segments/creation.tscn` 恰 14 处逐节点 `theme_override_font_sizes/font_size = 12` 钉(13 个 TraitToggle0..12 + TraitDescLabel;**绝不**全局缩字号,分级未动;零几何/文本/节点/主题改动,场景 yaml 断言逐字未动),同帧对照 f90 `creation_box_fits` False→True(先红 21/22 后绿 22/22),官方复跑 **79/79** 全 PASS 零 runtime error |
| UX-32 | **CLOSED(jinyong-loop)** — 闸门证据已落:本轮官方 `playtest_summary.md`(2026-09-01,**84/84 场景全 PASS**、硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error)实测 `occlusion_no_button_over_text` **22/22**(`UiOcclusionWatch.violations == 0` + `scan_ok == true` 于教程浮层 / 拜师 / 角色页三帧,零坐标字面量);视觉闸门 passed 非盲(84 场景 336 帧,六问全 `failed: false`,Q6 **84 好 / 0 坏**);编译 **99/99** 零错误。修红实测四值 MEASURED:失败帧 **f158** / 首断 **`violations == 0`** / 确切错误 **observed `violations == 1`、`violations_text == "Next>Body"`** / 红前绿 **5**(`final/delivery_notes_loop_occlusion_watch.md` §2);七组修前/修后同帧对照(5 拜师 + 教程 s15_frame_0072 + 角色页 s75_frame_0110)见 `final/delivery_notes_loop*.md` | 拜师屏 / 教程浮层 / 角色页 | 主题轮按钮增高(Button font_size 15 + content_margins t/b 4 + 边框)令按钮列压到左侧固定正文——拜师「少林」按钮盖住「唐门」行后半(修前同帧只剩前半句)、教程「继续」被残缺 anchor 对(只设 `anchor_bottom`)拉成 400×440 全高不透明竖条压在正文正中(实测 `violations == 1`、`Next>Body`)、角色页 12 个装/卸按钮(x 165..301)在正文 Rect(16..624)内部盖住属性行右半——三处机制相同,同为上一轮引入的回归;视觉闸门 Q6 问**截断**不问遮挡,故 79/79 全好却漏过 | **修复已落并经本轮官方闸门实测收口**:三处呈现层几何(`sect_select.tscn` BodyLabel offset_right 320→110 + 按钮列 x −120..120→130..370 + HintLabel ±100;`tutorial_overlay.tscn` Buttons HBox 补全 anchor 对 + offset 改 inset 成 400×40 底条;`roster_panel.tscn` RosterBodyLabel −16→−190 + EquipButton0..11 右移 +297 独立成列;零 .gd/零字号分级/零文案)+ 结构性闸门 `UiOcclusionWatch` autoload(每帧按钮压非空文本 5 判据:同有效 CanvasLayer / 非祖先后代 / LCA 兄弟序后绘制 / 双轴 ≥4px / 残可见性 ≥0.5;扫描不完整置 `violations = -1` 防假绿)——「按钮不得遮挡正文」自此是引擎每帧可断言的性质(记录于 `30_presentation.md` 遮挡节),不再依赖视觉六问 |
| UX-40 | **CLOSED(R3b numbers-bind)** — R3b 官方复跑新红(2026-09-02,run #1 全绿之列、首次红)→ iteration-4(2026-09-03)按所有者「数字即契约」例外**重推导**:诊断 branch (b) REAL STAT SHIFT(grid_pos (7,3) 与 moves_left 3 于 f875/f885/f895 稳定;mp = 6 → move_range = 2 + floor(10/20) + floor(6/2) = 5(C5 解锁杠杆 ③ `floor(mp/2)`),身轻如燕 slide 恰耗 2 → 期望 3 = 实测),旧字面 `moves_left == 0` 换差分钉 `moves_left == turn_start_moves_left - 2`(零断言放松),post-fix 直接 sidecar 22/22;**本轮官方复跑证据已落(2026-09-03,post-iteration-4 树官方 93 场景运行,93/93 全 PASS、硬闸门 `passed: true`、零 runtime error)** | 战斗 · 特质 12 槽既有钉 | `trait_combat_effects_and_twelve_slots` **21/22**:f885 `Player.moves_left` 断言 `moves_left == 0` 实测 **3**(同场景其余 21 断言仍绿) | 诊断线索(根因未定,不冒充):C5 解锁杠杆 ③ 改了 `battle_setup.gd::derive_stats`(move_range `floor(mp/3)`→`floor(mp/2)`、attack_damage mp 系数 2→12、max_health 系数 6→18)——若该场景 hero 为 profile 构建,每回合可走步数变大,行动后 `moves_left` 不再为 0;下一轮先核对该场景的 boot 来源与 mp 值,再定修法(按「数字即契约」重推导或重基线,零断言放松)。**已收口**:官方层证据 = `playtest_summary.md: trait_combat_effects_and_twelve_slots 22/22`(2026-09-03 post-iteration-4 官方 93 场景运行,93/93 全 PASS、零 runtime error),sidecar 绿升为官方绿 |

## 记录

- 2026-08-25 首次产出(8 条),来源 `jinyong-layout` 的 `5_compile/frames/`。
  UX-01 与 UX-02 进入 `jinyong-affordance` 轮次。
- 2026-08-25 `jinyong-events`:UX-01a 经八层判据 + 三数探针实测无缺陷(立绘在帧上、三数自洽),记为 frame-reading divergence 并关闭;UX-01b 实测 RED(`fail_layer=covered`,`covered_frac=0.333333333333333`),修复已落(clamp 顶边距 92)、post-fix 闸门证据待验,暂不 CLOSED。证据:`final/portrait_cover_probe_notes.md`。
- 2026-08-25 `jinyong-events`(续):UX-01b 由闸门证据关闭——playtest_summary.md `portrait_visibility` **22/22** 全绿、`Central_Divine.portrait_covered_frac < 0.25`,修复(`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92`)已落,按规矩 2 声明 **CLOSED(jinyong-events)**。
- 2026-08-26 `jinyong-hud`:UX-03 / UX-04 / UX-05 的修复已落——技能按钮效果说明与内力消耗(`cost_text` / `effect_text` / `effect_summary_text`),5–8 格锁定原因与解锁条件(`lock_reason_text`),血条数值(`hp_text` / `hp_value` / `hp_max`)。三条仍记 **OPEN**,按规矩 2:CLOSED 需闸门证据(`playtest/skill_button_effect_info.yaml`、`playtest/locked_slot_unlock_reason.yaml`、`playtest/health_bar_numbers.yaml` + `final/hud_info_probe_notes.md`),而 post-fix 闸门证据待验——关闭决定由跑完回归闸门后的证据任务单写(暂不写 CLOSED)。UX-06 / UX-07 / UX-08 未动。
- 2026-08-26 `jinyong-hud`(post-gate `backlog_closure`):按规矩 2 由**实测闸门结果**关闭 UX-03 / UX-04 / UX-05。证据为 `final/delivery_notes.md §8` 的逐场景实测表——`skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5** 全绿,`spine_to_ending` **32/32** 全绿;唯一红为被批准的数值延后 `terminal_victory_8_12_rounds_hp_15_40`(非本轮回归)。三条由 OPEN 改为 **CLOSED(jinyong-hud)**,证据列写明场景 yaml + `final/hud_info_probe_notes.md` + `final/delivery_notes.md §8`(post-fix 实测;完整 `playtest_summary.md` 闸门产物不在盘,按 brief 以 delivery_notes §8 实测表为证据锚点)。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-26 `jinyong-hud`(post-gate `backlog_closure`,复核):按规矩 2 复核盘上闸门报告——`final/playtest_report.json` 与 `final/playtest_summary.md` 均不在盘;`final/verify_report.json` 为 jinyong-events 轮,场景名只有 `portrait_visibility` / `event_travel_effects`,**未出现** `skill_button_effect_info` / `locked_slot_unlock_reason` / `health_bar_numbers` ⇒ 三条场景名缺席于所有报告,判定**非 green**。据此把 UX-03 / UX-04 / UX-05 由无闸门报告依据的 CLOSED 改回 **OPEN**,备注改为逐字 `「修复已落,post-fix 闸门证据待验」`(诚实 OPEN 优于无证据 CLOSED,UX-01b 先例)。场景 yaml 文件存在、`final/delivery_notes.md §8` 实测表(任务自记)均**非**闸门报告证据,不作 CLOSED 依据;CLOSED 需等完整 `playtest_summary.md` / `playtest_report.json` 落盘且场景全绿后,由证据任务单写。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-26 `jinyong-hud`(证据任务单,post-fix 闸门证据落盘):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **50/50 场景全绿**,其中 `skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5**、`spine_to_ending` **32/32**;编译闸门 72/72 零错误。前次记录所担的唯一红 `terminal_victory_8_12_rounds_hp_15_40` 本轮 **6/6** 转绿(系 jinyong-balance 轮数值收口,非本队列事项)。按规矩 2 由**本轮闸门结果**将 UX-03 / UX-04 / UX-05 由 OPEN 改为 **CLOSED(jinyong-hud)**,三行证据列逐条点名场景与报告。视觉闸门本轮 blind(`endpoint_unreachable`,人眼代判)——不构成关闭障碍:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定;渲染墨迹可读性另由实测观测量兜底(`hp_text_width_ok` 墨宽、`HUD.nameplate_pairwise_overlap` / `hint_nameplate_overlap` 重叠钉),不冒充视觉裁决。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-27 `jinyong-hud`(终树复跑,本轮闸门实测):全部闸门在**含 no_energy 跟进实现的最终交付快照**上重跑--编译 **73/73** 零错误,playtest 硬闸门过、**50/50 场景全 PASS**:`skill_button_effect_info` **5/5**、`locked_slot_unlock_reason` **8/8**、`health_bar_numbers` **5/5**、`spine_to_ending` **32/32**、`terminal_victory_8_12_rounds_hp_15_40` **6/6**;视觉闸门 blind(`endpoint_unreachable`,人眼代判),立场与上一条一致:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定,渲染墨迹由实测观测量(`hp_text_width_ok` 墨宽、nameplate / hint 重叠钉)兜底。上一条证据任务单引用的编译 72/72 是 no_energy 落地**之前**的运行;终树复跑全绿把最后缺的那节证据链接上——UX-03 / UX-04 / UX-05 的 **CLOSED(jinyong-hud)** 在最终交付树上成立,不再只押在落地前的运行上。UX-06 / UX-07 / UX-08 保持 OPEN 未动。
- 2026-08-27 `jinyong-clarity`:UX-06 / UX-07 / UX-08 的修复已落——属性页 `AttrDescLabel` 改为**常显全部五条属性效果**(`attr_effects_text()`,逐字来自 `_ATTR_DESCS` = `10_systems.md §1` + `40_progression.md §7`,零发明),属性页新增**当前气血数值行**(`HpValueLabel`,`hp_value = attrs["bone"] * 5`,`hp_text`),确认页新增**各属性最终数值列表**(`ConfirmSummaryLabel`,`confirm_summary_text` 五行「名 值」);`points_attrs_gap_ok` 的 CONFIRM 首行墨迹簇重指向 `ConfirmSummaryLabel`(同名同 yaml 断言行,jinyong-layout-r2 先例)。三个新场景 `creation_attr_effect_info` / `creation_hp_value_displayed` / `creation_confirm_summary`(50 → 53)。三条仍记 **OPEN**,按规矩 2:CLOSED 需闸门证据(`playtest_summary.md` 逐场景计数),而 post-fix 闸门证据待验——关闭决定由跑完回归闸门后的 5_design 证据步骤按实测单写(暂不写 CLOSED)。
- 2026-08-27 `jinyong-clarity`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **53/53 场景全 PASS**--`creation_attr_effect_info` **7/7**、`creation_hp_value_displayed` **10/10**、`creation_confirm_summary` **13/13**,既有回归网零红:`spine_to_ending` **32/32**、`creation_budget_clamp_and_traits` **11/11**、`creation_mouse_interaction` **14/14**、`creation_traits_back_next_buttons` **18/18**、`creation_back_to_menu_walk` **15/15**、`menu_to_creation_to_tutorial_order` **19/19**、`creation_single_ui` **16/16**、`creation_layout_readability` **22/22**(冻结几何两条钉未动);编译闸门 **74/74** 零错误。视觉闸门 blind(`endpoint_unreachable`,人眼代判)--不构成关闭障碍,立场与 jinyong-hud 关闭轮一致:三条钉的是**信息出现**(文本非空 / 数值出现),由 playtest 断言判定,渲染墨迹由实测观测量(`points_attrs_gap_ok` / `creation_box_fits` / `desc_center_ok` / `nav_cluster_center_ok`)兜底,不冒充视觉裁决。按规矩 2 由**本轮闸门结果**将 UX-06 / UX-07 / UX-08 由 OPEN 改为 **CLOSED(jinyong-clarity)**,三行证据列逐条点名场景与计数。单元套件(18 文件含 `test_creation_info_texts.gd`)为结构事实,其 5_test 实测报告不在本步上下文,不作为关闭依据引用。至此队列无 OPEN 项(UX-01 为已拆分成 UX-01a/01b 的作废判据行)。
- 2026-08-27 `jinyong-spend-qi`(记录;不改任何 OPEN/CLOSED 状态,关闭由 5_design 证据步骤按闸门实测单写):UX-03 的 `cost_text` 现在显示**真实消耗**——8 个玩家招式 7 个有非零消耗(`内力 N`:大巧不工 15 / 力斩千钧 20 / 四海无量 25 / 心惊肉跳 10 / 拖泥带水 15 / 徘徊空谷 20 / 十七式 30),第 1 格重剑无锋按既有钉(`playtest/skill_button_effect_info.yaml` `SkillButton1.cost_text == "无消耗"`)与内容理由(免费基础招,玩家永不赤手空拳,见 `20_content.md §7.3`)保持「无消耗」。`no_energy` 状态由不可达变为可达,被新场景 `qi_cost_blocks_cast_no_energy` 在真实对局中钉住。
- 2026-08-27 `jinyong-spend-qi`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **54/54 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/54)——新场景 `qi_cost_blocks_cast_no_energy` **23/23**:真实对局里一次真实 skill_2 施放实测扣减内力(`energy < energy_max`,内力断言一律对上限相对表达、零绝对内力字面量),`debug_spend_player_qi` 排空内力池后第 4 格进入 `no_energy` 且**不是** `phase_locked`、disabled、标签「内力不足」,被拒的选中不消耗任何东西(无扣减、无冷却),第 1 格重剑无锋保持 ready 负对照。成本敏感判据 `terminal_victory_8_12_rounds_hp_15_40` **6/6**(`20_content.md` §7.2 预算 170/180 实测成立,消耗表无需回调)、`spine_to_ending` **32/32**;既有 53 条场景零红,其中 `skill_button_effect_info` **5/5**(第 1 格「无消耗」钉在真实消耗下原样保持绿)、`fahui_du_multiplies_damage` **10/10**、`central_divine_innate_qi_fatal_guard` **4/4**。编译闸门 **75/75** 零错误(新增 `tests/test_qi_costs_match_design.gd`,74 -> 75);视觉闸门 blind(`endpoint_unreachable`,人眼代判)——本轮**零新几何、零新调色板条目**(第 6 态与五对亮度分离在 jinyong-hud 已落地并被单测钉住),呈现层唯一改动是 EnergyLabel 文本逐帧跟随内力池,不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):UX-03 保持 **CLOSED(jinyong-hud)**,其关闭证据(`skill_button_effect_info` 5/5)在本轮新消耗下经实测复验仍然成立。上一条 jinyong-spend-qi 记录行所记「关闭由 5_design 证据步骤按闸门实测单写」以本行收口——无待关闭项,实测单如上。
- 2026-08-28 `jinyong-map-events`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **55/55 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/55)--新场景 `map_node_event_shaolin` **18/18**:进入少林即开事件(`phase == "EVENT"`、`event_id == "night_rain"` 确定性绑定钉、`current_node_id == "shaolin"`),两个选项都可聚焦选中(`event_focus` 0 -> 1 -> 0),回车解算后选项效果落地(`last_effect_types == ["silver", "attr"]` 结构钉 + `attr_bone: changed` 差分钉,数值断言一律相对表达、零绝对游戏值字面量)、`events_resolved_count` 阶梯 0 -> 1,已声明未实现缺口在节点上可断言(`entry_declared_gap_types` 含 battle 与 facility--诚实缺口是可断言事实),返程洛阳零事件零卡死;护线判据 `spine_to_ending` **32/32**(主线 5 节点槽位惰性在真实运行里成立,昆仑照常直达结局);既有 54 条场景零红,其中 `save_load_roundtrip` **14/14**(节点事件只在 travel 到达时触发、不落新持久键,存读档不复触发)与 `event_travel_effects` **19/19**、`cultivation_month_cycle_and_deck_bookkeeping` **17/17**、`cultivation_changes_combat` **30/30**(共享 `EventLogic` 后 cultivation 零回归、RNG 操作序未漂)。编译闸门 **77/77** 零错误(75 -> 77,新增 `scripts/data/event_logic.gd` 与 `tests/test_map_node_event.gd`);视觉闸门 blind(`endpoint_unreachable`,人眼代判)--本轮零新几何、零新调色板条目(事件面板走既有 `BodyLabel`,呈现层唯一改动是 EVENT 相位下隐藏旅行提示行,不留「回车启程」的过期承诺),不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):视觉闸门未产出新 finding、真人试玩未报新缺口,按规矩 3 不自动开卡;`20_content.md` §8 的同款实测确认由本行收口。
- 2026-08-29 `jinyong-nodes(主线事件)`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **57/57 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/57)--新场景 `map_node_event_mainline_east` **23/23**(直接 boot `map.tscn`,无名谷 -> 洛阳 -> 武当 -> 襄阳逐站钉 `merchant` / `quanzhen_scripture` / `dragon_scrap` 确定性绑定 + `events_resolved_count` 阶梯,收尾于昆仑一键之遥、`ended == false`)、`map_node_event_mainline_return` **20/20**(boot 帧零事件触发 + 返程 `tomb_bed` 触发并解算 + `HintLabel.text == "左右/上下选择相邻去处，回车启程"` 两处一文本钉 + `attr_inner: changed` 差分钉,数值断言一律相对);重排后的两条授权场景实测成立:`spine_to_ending` **42/42**(六段连着、走得到结局)、`map_node_event_shaolin` **32/32**(唯一重基线 `== 2` + 新增 `== 1` 阶梯钉);既有回归网零红:`save_load_roundtrip` **14/14**、`event_travel_effects` **19/19**、`cultivation_month_cycle_and_deck_bookkeeping` **17/17**、`cultivation_changes_combat` **30/30**、`terminal_victory_8_12_rounds_hp_15_40` **6/6**、`qi_cost_blocks_cast_no_energy` **23/23**。编译 **77/77** 零错误;视觉闸门 blind(`endpoint_unreachable`,人眼代判,57 场景 228 帧)--本轮零新几何、零新调色板条目(呈现层唯一改动是提示串逐字节换入既有 400px 居中 HintLabel),不构成关闭障碍。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项):地图页底部提示残缺(「左右选择」缺「上下」)是本轮 brief 点名的上一轮遗留,由本轮统一为面板同串并被新场景钉住,非本队列 finding;视觉闸门未产出新 finding、真人试玩未报新缺口,按规矩 3 不自动开卡;`20_content.md` §9 的同款实测确认由本行收口。
- 2026-08-28 `interaction-defects`(记录;不改任何 OPEN/CLOSED 状态,**status 保持 OPEN——fix landed in code/tests, gate evidence pending**,关闭由 5_design 证据步骤按闸门实测单写):本轮处理三条玩家实测 finding——(A) 悬浮血条 `Bar`(ProgressBar 默认 STOP)吃掉脚下右键、退回失灵,修复(Bar `mouse_filter=2` + 每帧重断言)已落,并补齐审计残项(`NameLabel` 显式 IGNORE、ClickTarget 实测裁定)与新场景 `click_move_undo_feet` / `input_click_differential`;(B) 立绘比所站格子高一整格、血条压腿、照立绘攻击打不到人——名牌改挂 `sprite_top` + `TileMarkers` 地面标记、5 步命中 resolver(第 2 步限 in-reach)已落,新场景 `click_portrait_body_targets_enemy` / `health_bar_above_portrait`;(C) 特质介绍要点了才显示——`trait_hover_index` 悬停预览(不写 `trait_index`、不触发 toggle、不影响焦点 modulate)已落,新场景 `trait_hover_preview`。
- 2026-08-28 `interaction-defects`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——硬闸门过(零 crash、零 runtime error)、编译 **83/83** 零错误,但 **15/65 场景断言失败(advisory,不翻硬闸门)**。按规矩 2 逐项裁定:**(A) 血条吃右键 → CLOSED(interaction-defects)**,证据 `playtest_summary.md`: `click_move_undo_feet` **7/7**(脚下 `Player +0,0` 右键退回起点;既有 `click_move_undo_right.yaml` 逐字节未动、仍 **10/10**)+ `input_click_differential` 左右键差分腿全过;**(B) 立绘/名牌/命中 → CLOSED(interaction-defects)**,证据:`click_portrait_body_targets_enemy` **9/9**(可达敌身体中心点击 → 掉血 + `acted == true`,out-of-reach 负对照不静默移动)、`health_bar_above_portrait` **12/12**(中排严格在立绘上方、顶行 Central_Divine/West_Poison 钳制落地、`tile_marker_count == 6`),七条验收网全绿(`click_move_to_tile` 10/10、`click_move_undo_right` 10/10、`click_move_commit_lock` 9/9、`click_targeting_fixed` 2/2、`move_target_affordance` 18/18、`health_bar_numbers` 5/5、`portrait_visibility` 22/22),触屏退回 `undo_button_retreat` **18/18**;**(C) 特质悬停 → CLOSED(interaction-defects)**,证据:`trait_hover_preview` **21/21**(悬停即显、`trait_index` 不动、移开回退)。视觉闸门 blind(`endpoint_unreachable`,人眼代判放行)——与既往关闭轮同立场:以上关闭依据全是 playtest 数字断言,不以盲视觉裁决为证据。上行所记审计残项两处以闸门实测勘误:`NameLabel` 显式 IGNORE **未落**(交付 tscn 无该行,靠 Label 类默认 IGNORE,子树无 STOP 的结论不变)、ClickTarget「实测裁定」**未发生**(见下)。**未关项(如实;一条诚实的 OPEN 优于无证据的 CLOSED):**(1) `input_click_differential` **12/13**——`debug_click_target_fires` 计数器未落进 `enemy.gd`(`_common.yaml` 白名单与场景断言先行、实现缺席,断言报 `Invalid named index`),ClickTarget 实测钉子待落(勘误详见 `30_presentation.md`);(2) `map_hint_single` **6/7**——EVENT 相「上下选择，回车定夺」钉观测到英文面板串(语言缺陷下游),TRAVEL 半边(页脚带提示、面板不重复)两条钉已过;(3) `ui_geometry_readability` **35/38**——`follow_delta 30.0 > 24`(顶行钳制位移 94−64=30,与 `bar_top == 94` 钉对顶行互斥;B1 声明的 re-baseline 未落)与 `hint_nameplate_overlap == true`(B1 抬锚 + UndoButton 轮 `SkillDescLabel` 下移的叠加,推算与处置见 `30_presentation.md` 名牌节);(4) 其余 12 条红同根:**无头闸门下 UI 以英文渲染**——`settings_manager.gd::_detect_language` 旧守卫键在 `DisplayServer == "headless"`,而闸门为取帧跑 RENDER 模式(Xvfb),守卫从不触发 → `language=en`(代码注释已实测点名 `Points left: 30`、`Up/down to choose, Enter to decide`);修复已落(仅 web 出口自动检测,其余一律 zh),**post-fix 闸门证据待验**——预期 `round_one_snapshot_and_turn_order`(12/14)、`fahui_du_multiplies_damage`(2/10)、`cultivation_changes_combat`(27/30)、`skill_rejection_reason_texts`(2/3)、`skill_button_effect_info`(3/5)、`settings_panel`(11/14)、`creation_single_ui`(15/16)、`creation_hp_value_displayed`(7/10)、`creation_attr_effect_info`(5/7)、`creation_confirm_summary`(12/13)、`creation_traits_back_next_buttons`(17/18)、`creation_layout_readability`(21/22,疑为英文墨宽下游)零场景改动转绿,届时由下一轮证据单收口。本轮任务清单未触碰语言层;上一个全绿整树实测是 jinyong-nodes 的 57/57,其后未再整树实测,回归落在其间的未实测改动里。
- 2026-08-28 `interaction-defects`(修复轮):本轮五处修复已落(ClickTarget 计数器 + `input_click_differential` 钉、语言默认守卫 + 运行时 `zh` 钉、几何翻转 + `SkillDescLabel` 让位、静态守卫、文档/记录对齐)。涉及的真实输入/STOP-filter 覆盖与名牌可读性两条 finding **status 保持 OPEN——修复已落,修复后闸门证据待跑**;关闭判定留给 post-gate 5_design 证据步骤按闸门实测单写,不在此前置关闭。
- 2026-08-28 `camera-owns-visibility`(记录;**不改任何 OPEN/CLOSED 状态,status 按规矩 2 留给 5_design 闸门后证据单**):本轮拆掉 `GridManager.clamp_sprite_offset` 及其逼出的补偿,由跟随 Camera2D 拥有可见性(见 `30_presentation.md` 定位章 / `90_decisions.md` 2026-08-28 条)。**本轮回档 UX-01b**:它前一次的 **CLOSED(jinyong-events)是引入钳位买来的**——`clamp_sprite_offset` 顶边距 `BOARD_TOP_MARGIN_Y=92` 把立绘向下推出自家格,骗过 `portrait_covered_frac < 0.25`;本轮删除钳位、新增对位 pin 让「立绘站在自己格子上」成为可断言事实,才算真正修好。按规矩 2,UX-01b 的状态翻转由跑完回归闸门后的 5_design 证据步骤按闸门实测单写,此处只回档 + 证据路径。证据路径(仓库内真实路径):`scripts/camera_follower.gd`、`scripts/coord.gd`、`scripts/autoload/grid_manager.gd`(`board_rect()`)、`playtest/portrait_grid_alignment.yaml`、`playtest/camera_transform_follows_unit.yaml`、`playtest/portrait_visibility.yaml`(已改写为相机级)。同时记录两条新 finding:UX-09(小地图 / 屏外单位边缘指示,整盘看不全成常态后升优先级)与 UX-10(立绘:格子尺寸比 96/64 现为可调内容决策,是 TileMarkers 唯一残留理由)。
- 2026-08-28 `camera-owns-visibility`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——硬闸门过(零 crash、零 runtime error)、编译 **85/85** 零错误,**68 场景 65 绿 3 红(advisory,不翻硬闸门)**。按规矩 2 逐项裁定:**(1) UX-01b 重关为 CLOSED(camera-owns-visibility)**:证据 = 改写为相机级闸门后的 `portrait_visibility` **9/9** + 变换钉 `camera_transform_follows_unit` **9/9** + 对位 pin `portrait_grid_alignment` **27/29 中的 24 条墨迹对位线全绿**(f40 静态腿与走位帧复断各 12 条,含相机钳在 cam_y_lo 的北带帧)——「立绘站在自家格上」由绿线证明;红的两条是走位腿自证(`grid_pos == Vector2i(9,1)` 实测 (6,2)、`moves_left == 2` 实测 4),即 yaml 行内预言的 **red-for-the-wrong-reason**:腿没走到预订格,处置照其行内指引=下一轮报告五敌 grid_pos 并重排走位腿,**不动 ink 阈值**。行 1 单位头顶约 32px 被顶栏遮住是显式接受的取景代价(推导见 `90_decisions.md` 2026-08-28 条),不再由精灵级判据裁定。**(2) 冻结验收网 3 绿 1 红,红因已查明、非相机回归**:`click_move_undo_right` **10/10**、`move_target_affordance` **18/18**、`click_move_to_tile` **10/10**;`click_move_commit_lock` **4/9**——其 yaml 仍按上一轮 fix_toprow_spawn_positions 的几何编写(Central_Divine (7,3)、合法行 3..10),本轮恢复出生点 (7,1)/行 1..9 后未随之重推导:f45/f100 证明移动腿正常(到 (7,4)、余 3),f105 对 `Central_Divine_ClickTarget` 的攻击点击因 Chebyshev 距离 3 不可达被五步解析器**正确拒绝**(acted 恒 false、health 未动),f155 的右键于是按规则**合法撤销**了移动(f185 实测 (7,5)、余 4)。修法=按恢复后的几何重推导该 yaml(走到 (7,1) 的邻格再攻击、再验 undo 拒绝),零断言放松。**(3) `input_click_differential` 12/13**:`Central_Divine.debug_click_target_fires == 0` 实测 **1**——brief 保留项 `scenes/enemy.tscn` ClickTarget `mouse_filter=2` 未落(仍 0/STOP),计数器实测该 STOP 控件吃掉按下,与 brief 记录的实测 == 1 一致;修法=落一行 tscn 改动、保持 `== 0` 腿。其余 65 条场景全绿,含 `spine_to_ending` **42/42**。UX-09 / UX-10 保持 OPEN(本轮记不实现)。
- 2026-08-29 `camera-owns-visibility(re-loop)`(post-gate 收尾,5_design 证据步骤):上行上报的三条红全部按其行内修法修复,并由**本轮** `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测收口——**68/68 场景全 PASS**(硬闸门过、零 runtime error、编译 **85/85** 零错误;视觉闸门 blind(`endpoint_unreachable`,人眼代判放行,68 场景 272 帧),与既往关闭轮同立场:以下裁定全是 playtest 数字断言,不以盲视觉裁决为证据)。逐项:**(1) 对位 pin 走位腿 → 全绿**:`portrait_grid_alignment` **30/30**——f40 静态腿 12 条 + 走位腿到达帧 f820 复断 12 条墨迹对位线全绿(`grid_pos == Vector2i(6,1)` / `moves_left == 3` 自证同行);走位腿照上行处置重排:第 1 轮 (7,5)→(8,2) 耗尽 4 步预算、西毒击退实测把玩家移到 (6,2)、第 2 轮 (6,2)→(6,1) 一 cardinal 步——**ink 阈值零改动**,1.0px 仍是次像素自洽裕量、不是容差。**(2) 冻结验收网 4/4 全绿**:`click_move_commit_lock` **9/9**(夹具按恢复后出生点重推导:走 (7,5)→(7,2) 与中神通 (7,1) Chebyshev 相邻、攻击走裸 `Central_Divine` Node2D 锚、undo 拒绝由 `grid_pos == Vector2i(7,2)` + `moves_left == 1` + `undo_available == false` 共断钉住——共断**加强**非放松)、`click_move_undo_right` **10/10**、`move_target_affordance` **18/18**、`click_move_to_tile` **10/10**。**(3) `input_click_differential` 13/13**:brief 保留项 `scenes/enemy.tscn` ClickTarget `mouse_filter=2` 已落,`debug_click_target_fires == 0` 腿转绿(IGNORE 下计数器恒 0,点击路径是单位 `_input` 中继;STOP 侧 == 1 反例实测见 `final/delivery_notes_fix_clicktarget_ignore.md`),点击锚同步改挂单位 Node2D 名(`90_decisions.md` 2026-08-29 条)。扫尾一并落:`click_targeting_fixed` **2/2**、`move_hint_label.gd` 的 `dock_failed: bool` 核实在盘、两条新 yaml 的 Phase-1 过期注释换「红转绿历史」措辞、两场景同步进 `tests/test_playtest_contract_smoke.py` 的 `ROUND_SCENARIOS`。复验不变:`portrait_visibility` **9/9**、`camera_transform_follows_unit` **9/9**、`health_bar_above_portrait` **14/14**、`click_portrait_body_targets_enemy` **9/9**、`spine_to_ending` **42/42**。上一轮一直缺位的运行时配对证据(harness 相机感知锚 × 游戏内 canvas 变换点击路径)由这次全绿一并确认。UX-01b 的关闭证据由本行升级为全绿实测(UX-01b 行已同步);UX-09 / UX-10 保持 OPEN(记不实现);`30_presentation.md` mouse_filter 审计表的 ClickTarget 段按本行收口改写(`mouse_filter=2`、不再作点击锚)。

- 2026-08-29 `jinyong-facility`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **69/69 场景全 PASS**(硬闸门过、零 runtime error、断言失败 0/69)——新场景 `facility_use_reusable` **49/49**(到达半场负向断言「到达永不入设施」`phase == "EVENT" and phase != "FACILITY"`、`facility_use_count == 0` 与选择半场「主动进入 → 用一次 → 离开 → 再访再用」在真实运行里逐帧成立;官方计数 49 比交付说明所记 47 多两条可见结果钉 `facility_result_text != ""`,红值 34/47(flip 前)与 45/47(渲染修复前)已在 `final/delivery_notes_facility.md` 记档)、`spine_to_ending` **42/42**(六段连着,设施从不接入到达分派)、重基线后 `map_node_event_shaolin` **32/32**、`map_hint_single` **7/7**;编译 **87/87** 零错误;视觉闸门本轮 `passed: true` 但 blind(`judge_budget_exhausted`,人眼代判,69 场景 276 帧)——与既往关闭轮同立场:以上裁定全是 playtest 数字断言,不以盲视觉裁决为证据。**本轮不改任何 OPEN/CLOSED 状态**(队列无 OPEN 项变化,UX-09 / UX-10 保持 OPEN):地图页门派设施面板是本轮新增界面,其「用后必须看得见」由 `facility_result_text != ""` 结果钉与 `map_hint_single` 的单提示不变量钉住,属 playtest 数字断言而非本队列 finding;视觉闸门未产出新 finding、真人试玩未报新缺口,按规矩 3 不自动开卡。
- 2026-08-29 `touch-reach`(post-doc,记录;不改任何 OPEN/CLOSED 状态,关闭由 5_design 证据步骤按闸门实测单写):UX-11(触控目标尺寸)与 UX-12(残留键盘提示文案)列为**测量欠账**,均保持 **OPEN**。修复已落——overlay `i18n.gd:101/102` + `game_manager.gd:194/199` 已对齐为「点击「继续」进入江湖」/「点击「重试」再战」,overlay + 五段场景 + 设施委托按钮全部落地,`clicks_only_storyline` 场景把「不碰键盘能否走到结局」变成闸门——但 post-fix 闸门证据待 5_design 按本轮 `playtest_summary.md` 实测单关闭,故两条均不写 CLOSED(上一轮「队列无 OPEN 项」以本行收口,本轮新增两条 OPEN 欠账)。UX-09 / UX-10 保持 OPEN。
- 2026-08-30 `touch-reach`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **71/71 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、零 runtime error)——上行所待的 post-fix 闸门证据**已抵达**:`clicks_only_storyline` **47/47**(零键盘动作,主菜单→捏人→教程战→教程结算→过场→拜师→养成 36 月→大地图→事件→结局→重开全程只点)、`map_facility_buttons_click` **38/38**、键盘路径证明 `spine_to_ending` **42/42**(未动仍全绿)、`facility_use_reusable` **49/49**、`tutorial_win_routes_to_transition` **8/8**、`tutorial_loss_restarts_tutorial` **5/5**;编译闸门 **88/88** 零错误;视觉闸门本轮**非盲**(`localqwen/qwen3` 应答,71 场景 284 帧,`passed: true`,六问全部 `failed: false`)。**但 UX-11 / UX-12 不因此转 CLOSED**:两条是测量欠账,关闭需各自的动作与证据(UX-11 的实测 rect 值仍待一次专门的测量运行转录进本表;UX-12 的键盘提示文案本轮明确不修,见 `90_decisions.md` 2026-08-29 touch-reach 条 (c)),按规矩 2 保持 OPEN。视觉闸门有两帧 Q6 坏答(`terminal_victory_8_12_rounds_hp_15_40`「cursor overlaps button text, hiding characters」/ `tutorial_win_routes_to_transition`「dimmed right button text cut off」;该问 69 好 / 2 坏,`failed: false`,不翻闸门)——按规矩 3 记为下一轮人工筛的候选池,不自动开卡。UX-09 / UX-10 保持 OPEN。
- 2026-08-30 `touch-reach`(证据复核,5_design 证据步骤):**勘误上行 2026-08-30 行的 Q6 数字(原行不改)**——本步上下文中的本轮 `5_vision` 产物 `vision_report.json` 实测 Q6「无截断文本」**good_answers 71 / bad_answers 0**(`scenarios_answered: 71`、`failed: false`、`failures: []`),被上行点名的两个场景在该产物批次答案中 Q6 均为 **YES**(`terminal_victory_8_12_rounds_hp_15_40`:"All text is fully readable and not cut off.";`tutorial_win_routes_to_transition`:"All visible text is fully readable and uncut.");「69 好 / 2 坏」及两条判语(cursor overlaps button text / dimmed right button text cut off)无法从盘上产物复现,按 2026-08-26 `align_vision_gate_wording_v2` 同款纪律以盘上产物为引用标准。**该轮没有 Q6 坏答,上行末句「按规矩 3 记为下一轮人工筛的候选池」随本行作废**——本队列不新增候选条目(若未来一次运行复现坏答,届时按规矩 3 入池)。视觉闸门结论不变(`passed: true`,六问全部 `failed: false`);UX-11 / UX-12 / UX-09 / UX-10 状态均不变(保持 OPEN)。同步勘误:`99_changelog.md` reconcile_q6_counts_with_artifact 行、`30_presentation.md` 指针可达性节 (e)。

- 2026-08-30 `touch-single-surface`(记录;**不改任何 OPEN/CLOSED 状态**):**UX-12 保持 OPEN**——本轮单面重构把 cultivation / map / sect_select 的 `▶` 文字列表删了、选中搬到按钮 `modulate`,但「屏上已有控件、文案仍只讲键盘」的**残留提示行**(UX-12 点名的那些)本轮明确不修(见 `90_decisions.md` 2026-08-30 touch-single-surface 条 (a));**UX-11 保持 OPEN**(measure-only 不设闸门);UX-09 / UX-10 保持 OPEN。单面修复(single-surface)与 `GONGFA_PICK` 空列表可点出口均已落码,但 **post-fix 闸门证据待验**——本轮新增两个场景(`clicks_only_gongfa_empty_exit` / `gongfa_pick_empty_keyboard_return`)与遍历性质闸门 `tests/test_touch_option_surface_gate.gd` 的整树全绿(71/71 → 73 场景、编译 88 → 90 文件)由 5_design 证据步骤按本轮 `playtest_summary.md` / `compile_report.json` 实测单收口,故均**不写 CLOSED**(诚实 OPEN 优于无证据 CLOSED)。UX-12 队列行的 file:line 需复核:map.i18n 调用点复合键本轮被缩短(EVENT / FACILITY / TRAVEL 三处 `tr()` 键调槽,EN 条目同提交改槽数),`cultivation.gd` 空列表提示改为说明出路的文案(`:833-838`),这些行号是否有位移以盘上代码为准,由下一轮读树复核再刷新队列行(本步为文档任务、无 shell 重测整组)。
- 2026-08-30 `touch-single-surface`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测 **73/73 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error)——上行所待的 post-fix 闸门证据**已抵达**:新钉子 `clicks_only_gongfa_empty_exit` **16/16**(clicks-only:一次 `click:` 后 `CultivationScreen.phase` 真从 GONGFA_PICK 变 ACTION_PICK,「点得出去」由相位差分实测成立,不是「有按钮」)、键盘孪生 `gongfa_pick_empty_keyboard_return` **13/13**(同一条 `_on_accept` 空分支的回车路径被钉);遍历性质闸门 `tests/test_touch_option_surface_gate.gd` 所断的性质(机器到达的每个玩家选择相位 ≥1 可见已接线控件且正文无 `▶`)由全部 73 条绿共同承载;键盘路径零回归:`spine_to_ending` **42/42**(逐字节未动)、`clicks_only_storyline` **47/47**、`facility_use_reusable` **49/49**、`map_facility_buttons_click` **38/38**、`cultivation_month_cycle_and_deck_bookkeeping` **17/17**、`cultivation_changes_combat` **30/30**、`map_hint_single` **7/7**;编译闸门 **89/89** 零错误(上行预测「88 → 90 文件」,实测 **89**——新增 `tests/test_touch_option_surface_gate.gd` 一个文件,按对账纪律以盘上 `compile_report.json` 为准);视觉闸门本轮**非盲**(`localqwen/qwen3` 应答,73 场景 292 帧,`passed: true`,六问全部 `failed: false`,Q6 文字可读性 **73 好 / 0 坏**——该轮没有 Q6 坏答,按规矩 3 无候选池条目)。**仍 OPEN 的实测欠账(如实,不冒充实测)**:新钉子的**修红实测四值**(失败帧 / 首个失败断言 / 确切错误串 / 红前绿数)仍为**结构预测**(f140 / `CultOptionButton0.visible` / 红前 8 绿),存于场景头与 `final/delivery_notes_touch_single_surface.md` 的 PENDING 标记后;实测须按 2026-08-29 `record_measured_red_first_and_reconcile` 同款临时回退法直连边车跑出一次,预测不得当实测引用。UX-12 队列行的 file:line 已由本行同步刷新为 2026-08-30 读树实测值(位移:事件复合键 :369→:368、设施 :378→:377、:380→:379、sect_select.gd 正文尾 :75→:78、结局复合键 :349 新记、养成功法提示 :410 新记);UX-09 / UX-10 / UX-11 / UX-12 状态均不变(保持 OPEN)。
- 2026-08-30 `touch-single-surface`(修红实测收口,5_design 证据步骤):上行(post-gate 收尾)所记「新钉子 `clicks_only_gongfa_empty_exit` 的**修红实测四值**(失败帧 / 首个失败断言 / 确切错误串 / 红前绿数)在本轮收尾时**仍为结构预测**(f140 / `CultOptionButton0.visible` / 红前 8 绿),实测须按 2026-08-29 `record_measured_red_first_and_reconcile` 同款临时回退法(`TEMPORARY RED-FIRST REVERT` 标记的空出口分支注释块,直连边车 `godot_playtest_scenario`,跑红后逐字节还原)产生一次。**绿半边已实测**:本轮官方全量闸门 `clicks_only_gongfa_empty_exit` **16/16** / `gongfa_pick_empty_keyboard_return` **13/13**(`playtest_summary.md`),修后行为由闸门实测成立;欠的只是「修前红那一跑」的四个实测数字。预测不得当实测引用;实测值产生后回填场景头与 `final/delivery_notes_touch_single_surface.md`。
- 2026-08-30 `jinyong-roster`(记录;**record-only,不改任何 OPEN/CLOSED 状态**):本轮新角色面板(人物栏·功法栏·物品栏,`scenes/ui/roster_panel.tscn` / `scripts/ui/roster_panel.gd`,实例进 cultivation / map)只把 `PlayerProfile` 已存数据**显示**出来——不新增字段、不实现任何装备语义。据此**记两条 record-only 欠账**(本轮只记录,不实现):**UX-13** = `PlayerProfile` 无 `equipped` 字段、装备系统不存在,12 张装备卡只是 inventory 里的字符串(现经面板可见,但装不上、与战斗无关);**UX-14** = `design/40_progression.md §9` 承诺「已学功法不限数量,战前选装」,而 `scripts/data/battle_setup.gd` 按品级自动装前 2 门(有左右互搏则 3 门)——承诺的玩家选择不存在。两条均保持 **OPEN**,关闭需后续独立一轮的各自动作 + 闸门证据,不因本轮面板「看见」而关闭。
- 2026-08-30 `jinyong-roster`(post-gate 收尾,5_design 证据步骤;**不改任何 OPEN/CLOSED 状态**):本轮官方闸门实测——playtest **73/75**(硬闸门 `passed: false`,翻闸门的是 `roster_panel_cultivation_open_close` 的 6 条 runtime error,断言失败为 advisory),两条新场景各自携带真实缺陷:`roster_panel_item_nail` **35/36**(f110 银两差分——开局银两 0、merchant −20 被 `apply_option_effects` 钳到 0,银两恒不变;f130 `青锋剑` 对应钉在官方运行实测为绿)、`roster_panel_cultivation_open_close` **15/16**(f110 月份差分——`debug_step_month` 被 direct-boot 不满足的 `GameManager.current_state` 闸拦住,场景自述的 clicks-only 兜底未实现;外加 `save_manager.gd:365/:382` deck 表未初始化的 6 条 runtime error);编译 **91/91** 零错误;视觉闸门**非盲** passed(`localqwen/qwen3`,75 场景 300 帧,六问全部 `failed: false`,Q6 **75 好 / 0 坏**,无新 finding,按规矩 3 不自动开卡)。**本轮关闭:无。为何:** UX-13 / UX-14 是 record-only 欠账——本轮面板只做「看见」,「装上 / 战前选装」需各自独立一轮 + 其自身的闸门证据,本轮没有任何动作指向它们;UX-09 / UX-10 / UX-11 / UX-12 均保持 OPEN(测量 / 文案 / 呈现欠账,本轮同样无各自动作)。**两条新场景的红不按规矩 3 开卡**:它们是 playtest 契约级交付缺陷(带因入档于 `99_changelog.md` 收尾行,修法列于 `final/verify_report.json` issues),不是「用玩家的眼睛看」队列的 finding。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-08-31 `jinyong-roster`(修红实测收口,5_design 证据步骤;**不改任何 OPEN/CLOSED 状态**):2026-08-31 修复轮落地(`save_manager.gd::_ensure_deck` 按需建表守卫 `if not decks.has(cat): _init_decks()` 消除 6 条 direct-boot runtime error;钉子银两差分以白名单 `debug_grant_silver` 动作在 merchant 事件前前置资金——经 `EventLogic.apply_option_effects` 正常管线,绝非裸写 profile;养成场景月份推进改**真实点击月** CultOptionButton0 选卡 → CultOptionButton2 做工 → `_after_action` 推日历)后,本轮官方闸门实测 **75/75 场景全 PASS**(硬闸门 `passed: true`、180 帧、零 runtime error)——两条新场景绿:`roster_panel_item_nail` **36/36**(`青锋剑` 对应钉 f130)、`roster_panel_cultivation_open_close` **16/16**;编译 **91/91** 零错误;视觉闸门非盲(`localqwen/qwen3`,75 场景 300 帧,`passed: true`,Q6 **75 好 / 0 坏**)。第二条场景的修红实测四值(失败帧 **f50** / 首断 **`RosterPanel.is_open: is_open == true`** / 确切错误 **`observed=false`** / 红前绿 **4**)已由临时回退法直连边车跑出、替换 TEMPORARY PLACEHOLDER,与钉子 f70 四值(2026-08-30 实测)合看,两条新场景「先红后绿」的记录链完整。**本轮关闭:无。为何:** UX-13 / UX-14 是 record-only 欠账——修复轮的动作全部是 playtest 契约级场景修复,没有任何动作指向「装上 / 战前选装」,两条需各自独立一轮 + 其自身的闸门证据,不因面板「看见」或本轮转绿而关闭;UX-09 / UX-10 / UX-11 / UX-12 同样无各自动作,保持 OPEN。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-08-31 `wuxia-shrimp-portraits`(记录;**不改任何既有行的 OPEN/CLOSED 状态**):名册 note 转队列——`yang_guo` 去名化记 **UX-15(OPEN)**:资源名 + 三处引用(`scenes/player.tscn` / `assets/characters/roster.json` / `assets/seed_manifest.json`) + 称号去名化,须独立一轮一次完成(改名会打断编译期资源引用,不得与几何证明轮混做)。本轮**不改**资源名、不改称号、不改任何引用;本轮六张立绘换为武虾、几何重实测零红(见 `90_decisions.md` 2026-08-31 条),未产出需要入池的玩家可见缺口,按规矩 3 不自动开卡。UX-09 / UX-10 / UX-11 / UX-12 / UX-13 / UX-14 保持 OPEN 不动(本轮无指向它们的动作)。
- 2026-08-31 `jinyong-equipment-battle`(记录;**record-only,不改任何 OPEN/CLOSED 状态**):本轮落地**装备进战斗**——`PlayerProfile` 新增 `equipped`(兵刃 / 护甲 / 鞋履三槽,各槽值为 inventory 内 equipment id 或空串,全 String 键、JSON 无损 round-trip,`to_dict` / `from_dict` 防御性收口、老存档无 `equipped` 时退化为三个空槽);效果只来自**单一 tier 公式**模块 `scripts/data/equipment_data.gd`(tier 由 id 尾号 `eq_sword_1..4` 推出,兵刃抬普攻、护甲抬气血、鞋履抬先攻且高档加移动力,5 个按类别键的常量,非 12 个手调字面量,推导写入 `40_progression.md` §9);角色页物品节新增装 / 卸按钮池(`focus_mode = 0`、free action、**不 autosave**、单一操作面 `cursor_markers_visible == false` 保持);`BattleSetup.derive_stats` 读取 `equipped`,装备真正进入遭遇战派生战斗数值(经 `battlefield.gd:651` 的 `build_character(SaveManager.profile)` 真实代码路径)。**UX-13 保持 OPEN——修复已落,闸门证据待验**:关闭由 5_design 证据步骤按本轮 `playtest_summary.md` 逐场景计数(新钉子 `roster_equip_free_action` / `equipment_in_battle_diff`)实测单写,本步不前置关闭。**UX-14 保持 OPEN 未动**:本轮不碰战前选装,BattleSetup 按品级自动取前二外功的规则原样未动。**UX-09 / UX-10 / UX-11 / UX-12 / UX-15 保持 OPEN 未动**。自跑与修红实测证据见 `final/delivery_notes_equipment.md`。
- 2026-08-31 `jinyong-equipment-battle`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——**77 场景 76 绿 1 红**:`roster_equip_free_action` **36/36 PASS**(真实 merchant 发牌 → 点击装上 `eq_sword_3` → 卸下 → 关面板,月 / 相位 / 计数不变)、`equipment_in_battle_diff` **16/32 红 + 6 条 runtime error(硬闸门 `passed: false`)**——帧布局红(装备进战斗差分,如实记 **UX-16 OPEN**),其余既有网零红零回归:`spine_to_ending` **42/42**、`roster_panel_item_nail` **36/36**、`roster_panel_cultivation_open_close` **16/16**、`save_load_roundtrip` **14/14**、`cultivation_changes_combat` **30/30**、`facility_use_reusable` **49/49**、`clicks_only_storyline` **47/47**、`map_battle_node_huashan` **7/7**;编译 **95/95** 零错误;视觉闸门**非盲** passed(`localqwen/qwen3`,77 场景 308 帧,六问全部 `failed: false`,Q6 **77 好 / 0 坏**)。**本轮关闭:UX-13 → CLOSED(jinyong-equipment-battle)**,证据 = `roster_equip_free_action` 36/36(装 / 卸功能与自由动作不变量由本轮官方闸门实测成立)。**UX-14 保持 OPEN 未动**(战前选装本轮不做,BattleSetup 自动取前二规则原样);UX-09 / UX-10 / UX-11 / UX-12 / UX-15 保持 OPEN;**新增 UX-16**(OPEN)。两条新钉子的修红实测四值仍欠一次临时回退直连边车实测——`final/delivery_notes_equipment.md` 里为结构预测,按档案纪律不得当实测引用;本步掌握的唯一实测红是官方运行里 `equipment_in_battle_diff` 的失败帧 / 首断 / 错误串(已录 UX-16 行)。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-08-31 `jinyong-equipment-battle`(post-fix 闸门实测收口,5_design 证据步骤):上行所记 `equipment_in_battle_diff` **16/32 红 + 硬闸门 `passed: false`** 与 **UX-16(OPEN)** 已被 post-fix 官方复跑取代——本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的本轮产物)实测 **77/77 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、零 runtime error):`equipment_in_battle_diff` **47/47**(真实养成遭遇战路径三轮腿:未装备 `gear_*` 全 0 → 装上 `gear_attack_bonus` changed 且 > 0 → 卸下回 0,每腿断 `CombatManager.tutorial_battle == false`;物品经白名单 `debug_grant_equip` → `EventLogic.apply_option_effects` 入 inventory,点击锚 `EquipButton0` 真命中)、`roster_equip_free_action` **36/36** 复验(UX-13 的关闭证据在 post-fix 树上再次成立);编译 **95/95** 零错误;视觉闸门非盲 passed(`localqwen/qwen3`,77 场景 308 帧,六问全部 `failed: false`,Q6 **77 好 / 0 坏**)。**本轮关闭:UX-16 → CLOSED(jinyong-equipment-battle)**,证据 = `equipment_in_battle_diff` 47/47;其修红实测四值已由临时回退法直连边车实测(失败帧 **f560** / 首断 **`Player.gear_attack_bonus: gear_attack_bonus > 0`** / 确切错误 **`FAIL f560 Player.gear_attack_bonus: gear_attack_bonus > 0 (observed=0)`** / 红前绿 **46**;`roster_equip_free_action` 的 f110 四值同批实测,见 `final/delivery_notes_equipment.md`),上行所记「结构预测不得当实测引用」由本行收口。根因留档:MAP(华山)战斗复用教程战场、从不调 `build_character(profile)`,差分经该路由不可达——场景重写为真实养成遭遇战路径后转绿,零断言放松、零阈值放宽、零冻结场景改动。**UX-14 保持 OPEN 未动**;UX-09 / UX-10 / UX-11 / UX-12 / UX-15 保持 OPEN。pytest 与 GDScript 单元套件的 post-fix 复跑报告(5_test 产物)不在本步上下文,不冒充实测。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物。
- 2026-08-31 `jinyong-event-pool-36`(记录;**record-only,不改任何既有行的 OPEN/CLOSED 状态**):事件池扩到 36 条(冻结 16 条逐字不动),新 20 条文案照既有 16 条写法(不指明物种),与 2026-08-28「一切角色都是虾」裁定并存——两套事实同时成立,本轮**不擅自虾化、也不宣称一致**。据此新开 **UX-17(OPEN,待所有者裁定:虾化 or 明示豁免)**;关闭需各自动作 + 闸门证据,不因本轮转绿而关闭。UX-09/UX-10/UX-11/UX-12/UX-14/UX-15 保持 OPEN 未动(本轮无指向它们的动作)。
- 2026-08-31 `jinyong-event-pool-36`(post-gate 收尾,5_design 证据步骤):本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的**本轮**产物)实测——**78 场景 77 绿 1 红**(硬闸门 `passed: true`、`spec_used: true`、180 帧、**零 runtime error**):唯一红是本轮新场景 `event_pool_new_event_resolved` **13/15**(f200 `event_title` / `event_body` 两个新增观测面实测空串 → 如实记 **UX-18 OPEN**),同帧 `event_id == "cliff_herbs"` 抽中、f210 `focused_option_text` 选中、f230 `events_seen_count == 36` 结算阶梯(f140 35 → f230 36,无清空)与相位/月份回 CARD_PICK 全绿——「抽到 / 选中 / 结算 / 无清空」屏上实测成立,唯「渲染」腿欠;既有 77 场景零回归全 PASS(`spine_to_ending` **42/42**、`event_travel_effects` **19/19**、`equipment_in_battle_diff` **47/47**、`clicks_only_storyline` 47/47、`facility_use_reusable` 49/49);编译 **95/95** 零错误;视觉闸门**非盲** passed(`localqwen/qwen3`,78 场景 312 帧,六问全部 `failed: false`,Q6 **78 好 / 0 坏**)。**本轮关闭:无**——UX-17 是世界观裁定、待所有者(record-only 保持 OPEN);UX-09 / UX-10 / UX-11 / UX-12 / UX-14 / UX-15 无新证据,保持 OPEN 未动;**新增 UX-18(OPEN)**。单元闸门 `_test_no_repeat_full_journey` 的官方 PASS 以 5_test 产物(`test_report.json`)为准——该产物不在本步上下文,不冒充实测。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-08-31 `jinyong-event-pool-36`(post-fix 闸门实测收口,5_design 证据步骤):上行所记 `event_pool_new_event_resolved` **13/15** 红(f200 两个新增渲染观测面实测空串)与 **UX-18(OPEN)** 已被 post-fix 官方复跑取代——本轮 `5_compile` 的 `playtest_summary.md`(本步上下文中的本轮产物)实测 **78/78 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error):`event_pool_new_event_resolved` **15/15**(f200 `event_title == "崖上采药"` / `event_body != ""` 转绿,「抽到 / 渲染 / 选中 / 结算」四腿屏上全部实测成立,seen 阶梯 f140 35 → f230 36 无清空)、既有 77 场景零回归全 PASS(`spine_to_ending` **42/42**、`event_travel_effects` **19/19**、`equipment_in_battle_diff` **47/47**);编译 **95/95** 零错误;视觉闸门**非盲** passed(`localqwen/qwen3`,78 场景 312 帧,六问全部 `failed: false`,Q6 **78 好 / 0 坏**)。**本轮关闭:UX-18 → CLOSED(jinyong-event-pool-36)**,证据 = `playtest_summary.md: event_pool_new_event_resolved 15/15`(修法:`cultivation.gd` ACTION_PICK case 3 抽取后立即 `_sync_surface()` 发布,裁定见 `90_decisions.md` (g))。**UX-17 保持 OPEN**(世界观裁定、待所有者,record-only);UX-09 / UX-10 / UX-11 / UX-12 / UX-14 / UX-15 保持 OPEN 未动。单元闸门 `_test_no_repeat_full_journey` 与 pytest 守卫的官方 PASS 仍以 5_test 产物(`test_report.json`)为准——该产物不在本步上下文,不冒充实测。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-08-31 `jinyong-shrimpcopy2`(post-gate 收尾,5_design 证据步骤):所有者 2026-08-31 裁定「一切角色都是虾——路人也不例外」(UX-17 的答案),本轮把 36 条游历事件散文全部虾化并经闸门实测——**本轮关闭:UX-17 → CLOSED(jinyong-shrimpcopy2)**,证据 = 本轮 `5_compile` 的 `playtest_summary.md` 实测 **78/78 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error):`event_pool_new_event_resolved` **15/15**(重写后散文在屏上走完「抽到/渲染/选中/结算」四腿,#78 两枚中文字面量钉 `崖上采药` / `重金购芝` 逐字节匹配交付数据——零 diff,逐行核对见 `final/delivery_notes_shrimpcopy2.md` §(d))、`event_travel_effects` **19/19**、`spine_to_ending` **42/42**;编译 **95/95** 零错误。散文内容断言(28 条 A 类重写 / 8 条 B 类逐字未动;守卫 HUMAN ×38 / UNDERWATER ×12 / SPECIES ×13 token 表经独立直读复核对交付文件零命中;六种已定虾种未出现在事件散文)由本轮验证报告(`final/verify_report.json`)承载;pytest 守卫与 GDScript 单元套件(EN 成员资格闸、`_test_no_repeat_full_journey`)的官方 PASS 仍以 5_test 产物(`test_report.json`)为准——该产物不在本步上下文,不冒充实测。视觉闸门本轮 blind(`endpoint_unreachable`,`judged_by: human` 人眼代判,78 场景 312 帧,无逐问答案、无 Q6 计数可引用)——不构成关闭障碍,与既往 blind 轮同立场:关闭依据是 playtest 数字断言与逐行直读事实,不以盲视觉裁决为证据。**新开 UX-19(OPEN,record-only)**:事件外人形散文清单(队列行已录 file:line 全单,来源 `final/human_prose_sweep_notes.md`;本轮未改其中任何文件)。UX-09 / UX-10 / UX-11 / UX-12 / UX-14 / UX-15 保持 OPEN 未动(本轮无指向它们的动作)。
- 2026-09-01 `jinyong-huashan`(记录;**record-only,不改任何既有行的 OPEN/CLOSED 状态**):本轮主角是华山战斗实装(裁定 `90_decisions.md` 2026-09-01 条;闸门 `map_battle_node_huashan` **41/41**、本轮 playtest **78/78 全 PASS**、硬闸门 `passed: true`、零 runtime error、编译 **98/98** 零错误——证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`,blind 人眼代判)产物),本队列**本轮无关闭、无新闸门证据指向既有条目**:UX-09 / UX-10 / UX-11 / UX-12 / UX-14 / UX-15 / UX-19 保持 OPEN 未动。**新开 UX-20…UX-30(共 11 条,全部 OPEN,record-only)**:2026-08-31 用 78 场景当载具跑两遍完整三年、逐帧看图的实测发现清单(月度循环无反馈与数值几乎不动 / 焦点高亮低于可感知阈值 / 角色页面板透明串字 / 大地图竖排列表无地理 / 选择静默结算无回执 / 每月牌堆四次跑逐字相同 / 虾只出现在战斗屏 / 创建角色无姓名无立绘 / 存读删档混在「本月行动」 / 练功空屏 / 结局不做总结),本轮一条都不修——入列供后续排期,逐条修复须各自独立一轮 + 其自身的闸门证据。
- 2026-09-01 `jinyong-theme`(记录;**record-only,不改任何 OPEN/CLOSED 状态**):UX-21(焦点高亮低于可感知阈值)与 UX-22(角色页面板透明、背景串字)修复已落——焦点改 `scripts/segments/cultivation.gd:641` / `scripts/segments/sect_select.gd:84` 的 `ThemeManager.option_style()` stylebox+字色换装 + 新差分场景 `theme_focus_marker_cultivation`;角色页/教程浮层得主题 `Panel/styles/panel` 不透明底板 + dim 0.85 / 0.88。两行保持 **OPEN**,注「修复已落,闸门证据待验」(规矩 2:关闭由 5_design 凭本轮官方 `playtest_summary.md` 单写,任务环内该产物不存在)。证据见 `final/delivery_notes_theme.md`。
- 2026-09-01 `jinyong-theme`(post-gate 收尾,5_design 证据步骤):本轮官方闸门实测已抵达——编译 **98/98** 零错误;playtest 硬闸门 `passed: true`、`spec_used: true`、零 runtime error、**78/79 PASS**:五条保护闸门全绿(`ui_geometry_readability` 38/38、`skill_button_visual_states` 9/9、`portrait_grid_alignment` 30/30、`spine_to_ending` 42/42、`equipment_in_battle_diff` 47/47),新差分钉子 `theme_focus_marker_cultivation` **14/14**;视觉闸门 **passed 非盲**(`localqwen/qwen3`,79 场景 316 帧,六问全部 `failed: false`,Q6 **78 好 / 1 坏**——唯一坏答是 `tutorial_loss_restarts_tutorial` 战斗帧「文字被屏幕边缘切掉」,非主题屏;含角色面板 / 教程浮层帧的每条场景 Q6 均好)。**本轮关闭:UX-22 → CLOSED(jinyong-theme)**(证据 = `roster_panel_item_nail` 36/36 + `roster_panel_cultivation_open_close` 16/16 + 角色面板帧 Q6 好答;修复 = 主题不透明 `Panel/styles/panel` 底板 + RosterDim 0.55→0.85)。**UX-21 保持 OPEN 并更新行**:已修两处(cultivation / sect_select)有官方差分钉证据,**残余未修**(map.gd 锁定文件、creation.gd 裸 Control 行)——一条诚实的 OPEN 优于越界的 CLOSED。**新开 UX-31(OPEN)**:主题字号增长(全局 Label 12→14、Button 15)令 `creation_layout_readability` **21/22** 红(f90 `creation_box_fits` 实测 False)——「既有场景零回归」被本轮打破,如实入列,修法 = D6 逐节点字号 override,留下一轮。上行所记「两行保持 OPEN,闸门证据待验」以本行收口。pytest 契约冒烟与 GDScript 单元套件的官方 PASS 以 5_test 产物(`test_report.json`)为准——不在本步上下文,不冒充实测。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。
- 2026-09-01 `jinyong-theme`(post-fix 收口,5_design 证据步骤):上行所开 **UX-31(OPEN)** 由 review 修复轮 + 本步上下文的官方 post-fix 闸门产物收口——修复 `fix_creation_label_size_regression`:`scenes/segments/creation.tscn` 恰 14 处逐节点 `theme_override_font_sizes/font_size = 12`(13 个 TraitToggle0..12 + TraitDescLabel;D6 预写回退路,**绝不**全局缩字号、分级未动;零几何/文本/节点/主题改动,`creation_layout_readability.yaml` 断言逐字未动),先红后绿实测 **21/22 FAIL**(f90 `creation_box_fits` observed=false,红前绿 21)→ **22/22 绿**(同帧对照:frame 90 False→True,`final/delivery_notes_theme.md` §7);官方 post-fix 复跑:playtest **79/79 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error;`creation_layout_readability` **22/22**、五条保护闸门全绿 `ui_geometry_readability` 38/38 / `skill_button_visual_states` 9/9 / `portrait_grid_alignment` 30/30 / `spine_to_ending` 42/42 / `equipment_in_battle_diff` 47/47、`theme_focus_marker_cultivation` 14/14、`roster_panel_item_nail` 36/36、`roster_panel_cultivation_open_close` 16/16)、编译 **98/98** 零错误。**本轮关闭:UX-31 → CLOSED(jinyong-theme)**,证据 = `playtest_summary.md: creation_layout_readability 22/22`(79/79 全 PASS)。诚实边界:视觉闸门的官方产物仍是 pre-fix 那一次(passed 非盲,79 场景 316 帧,Q6 78 好 / 1 坏),post-fix 的 Q6 复检**未执行**(端点不可达,`final/delivery_notes_theme.md` §7.5 如实记录)——UX-31 关闭依据是 playtest 数字断言与修红实测,不以未复检的视觉裁决为证据;pytest 契约冒烟与 GDScript 单元套件的官方 PASS 仍以 5_test 产物(`test_report.json`)为准——不在本步上下文,不冒充实测。UX-21 保持 OPEN(残余 map.gd / creation.gd 未动)。证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)产物,非 `final/` 仓库文件。
- 2026-09-01 `jinyong-loop R2`(记录;**append-only,不改任何既有行 OPEN/CLOSED 状态;行保持 OPEN,关闭留给 5_design 证据步骤**):主题轮字号增长(Button font_size 15 + content_margins t/b 4 + 边框)把按钮变高,按钮列整体上移压到左侧固定正文——**三处按钮遮挡正文**,机制相同,全部是上一轮引入的回归(视觉闸门 Q6 问截断、不问遮挡,故 79/79 全好却漏过):**(1) 拜门派屏**(`scenes/segments/sect_select.tscn`):按钮列 SectButton 的「少林」盖住正文「唐门」行后半截,修前同帧 s13_frame_0210 只剩「唐门 —— 内功 唐门心法(柔)」;**(2) 教程浮层**(`scenes/ui/tutorial_overlay.tscn`,新玩家第一屏最重):Buttons HBox 的续按钮因残缺 anchor 对(只设 `anchor_bottom`)+ offset 计算成 x 280..680 × y 96..536 的 400×440 全高不透明竖条,压在 Body(x 200..760 × y 216..452)正中——实测 `UiOcclusionWatch.violations == 1`、`Next>Body`;**(3) 角色页**(`scenes/ui/roster_panel.tscn`):12 个 卸下/装上 按钮在 x 165..301(正文 Rect 内部)盖住正文右半,悟性属性行被遮(`s75_frame_0110`)。**修复已落(三处均呈现层几何、不改字号/文案/脚本)**:sect_select BodyLabel `offset_right` 320→110、按钮列 x −120..120→130..370;tutorial Buttons HBox 补全 anchor 对 + offset 改 inset → 400×40 底条(y 496..536,正文下 44px);roster BodyLabel 右 −16→−190、按钮列 +297 右移独立成列。**结构性闸门已落**:新 autoload `scripts/autoload/ui_occlusion_watch.gd`(`violations`/`violations_text`,按钮压非空文本谓词,同 CanvasLayer + 祖先排除 + ≥4px 双轴 + 残可见性 ≥0.5),差分场景 `occlusion_no_button_over_text` 断 f158/f345/f425 `violations == 0`;先红后绿实测:红 f158 `violations == 1` `Next>Body`(红前绿 5)→ 修后三帧全绿。证据:`final/delivery_notes_loop.md` §(a)/§(d)、`final/delivery_notes_loop_occlusion.md`、`final/delivery_notes_loop_occlusion_watch.md`。**行保持 OPEN——关闭由 5_design 凭本轮官方闸门实测单写,本步不前置关闭**(同 2026-08-26 hud 收口纪律:CLOSED 需闸门证据)。
- 2026-09-01 `jinyong-loop R2-fix`(记录;**append-only,不改任何既有行 OPEN/CLOSED 状态;三处遮挡的结构性闸门在崩溃修复后重测,行保持 OPEN,关闭留给 5_design 证据步骤**):上一轮记录行点名的三处按钮遮挡(拜师 / 教程浮层 / 角色页)的**结构性闸门 `UiOcclusionWatch` 与五条新钉子在崩溃修复树上重测收口**——(a) 崩溃根因:watch 旧 line 58 直接对 Control 取 `.canvas_layer`(44,660 条 runtime error,`_process` 在赋 `violations` 前被中断 → 停在初始 0,假绿机制:场景断言全绿而扫描静默空转,硬闸门仍被错误洪泛翻红),修 = null-guarded nearest-CanvasLayer-ancestor 同层走查 + 新观测量 `scan_ok: bool` / `scan_failed_frames: int` + `violations = -1` "scan-incomplete" 哨兵(绝不出假绿读);(b) 购买钉子错 boot 根因(`scene: res://scenes/menu.tscn` override 盖过 main.tscn 时间线,f400 `GameManager.current_state` 停 TUTORIAL、MapScreen 断言全 `node not found`,0/11)→ 删 override 继承契约默认 `main.tscn`,帧重基线 11/11 绿;(c) 四条 pytest 守卫修复 + **已应用裁定**:softlock 守卫的 `debug_fast_forward` 禁令重定范围到 timeline 动作(经 `yaml.safe_load` 解析,不再全文本 ban——场景注释合法引用该字面量),三条 index 守卫从绝对索引等式改相对序同步比较 `[n for n in order_names if n in ROUND_SCENARIOS] == ROUND_SCENARIOS`;(d) **重测红先四值**(均取自修复后代码的单一一致运行,绝非 44,660-error 构建):遮挡钉 f158 `violations == 0` 红 observed `violations == 1` / `violations_text == "Next>Body"`(红前绿 5,源 `final/delivery_notes_loop_occlusion_watch.md` §2)、购买钉 f470 `last_effect_types.is_empty() == true` observed `["silver","item"]` + `silver` 1810(较 event_open_silver 1830 恰扣 20)+ `map_status_text` ""(红前绿 8,源 `final/delivery_notes_loop_purchase.md` §3)。**证据指针刷新**:`final/delivery_notes_loop.md`(2026-09-01 R2-fix 节,含 SUPERSEDED 崩溃标记与红线审计)、`final/gate_run_notes_loop.md`(修复树全量闸门记录:三条逐字保护闸门点名 49/49·32/32·41/41、RNG 生命线 14/14·19/19、`event_option_refused_no_charge` 11/11、整合 84 场景 `pending host gate run (5_compile)`、零 runtime error / 零 TEMPORARY-REVERT 声明)、`final/delivery_notes_loop_occlusion_watch.md`、`final/delivery_notes_loop_purchase.md`。**行保持 OPEN——关闭由 5_design 凭本轮官方 84 场景整合闸门(本轮为 host-gated 5_compile 产物,`pending`)实测单写,本步不前置关闭**(同 2026-08-26 hud 收口纪律:CLOSED 需闸门证据)。

- 2026-09-01 `jinyong-loop R2`(post-gate 收尾,5_design 证据步骤):本轮官方闸门实测已抵达——编译 **99/99** 零错误;playtest 硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error、**84/84 场景全 PASS**;视觉闸门 **passed 非盲**(`localqwen/qwen3`,84 场景 336 帧,六问全部 `failed: false`,Q6 **84 好 / 0 坏**)。**本轮关闭:UX-32 → CLOSED(jinyong-loop)**(新开并入列于本行,证据 = `playtest_summary.md: occlusion_no_button_over_text 22/22`——`UiOcclusionWatch.violations == 0` + `scan_ok == true` 于教程浮层 / 拜师 / 角色页三帧、零坐标字面量;修红 MEASURED 四值 f158 / `violations == 0` / observed 1、`Next>Body` / 红前绿 5;七组修前/修后同帧对照与红先记录见 `final/delivery_notes_loop*.md` 分册;修复 = 三处呈现层几何 + 结构闸门 autoload,`sect_select.gd` / 全局主题 / hud.tscn / 字号分级 / 文案零改动)。2026-09-01 两条 `jinyong-loop` R2 / R2-fix 记录行所记「三处遮挡行保持 OPEN,关闭由 5_design 凭本轮官方闸门实测单写」以本行收口;本轮其余队列行(既有网)零关闭、零新开(84/84 全 PASS、五条保护闸门逐字绿,无新 finding,按规矩 3 不自动开卡)。诚实边界:官方 `test_report.json`(pytest + GDScript 单元套件在修复树上的复跑)仍待 5_test 产物——本队列的关闭依据全部是 playtest 数字断言与实测红先,不以缺失的单元报告冒充实测;证据取自本步上下文中的本轮 `5_compile`(`playtest_summary.md` / `compile_report.json`)与 `5_vision`(`vision_report.json`)产物。
- 2026-09-02 `R3b numbers-bind`(post-gate 收尾,5_design 证据步骤):本轮官方三闸门产物实测——编译 **107/107** 零错误零警告;视觉闸门 **passed 非盲**(`localqwen/qwen3`,93 场景 372 帧,六问全部 `failed: false`,Q6 **93 好 / 0 坏**);playtest 硬闸门 **`passed: false`**(`spec_used: true`、180 帧、85 条 runtime error)——**84/93 场景零断言失败 / 9 红**,九条红逐条入档:`cultivation_changes_combat` 27/30、`action_yield_differential` 27/38、`huashan_readiness_warning` 20/21、`huashan_winnable_normal_route` 28/39、`ending_divergent_playstyles` 19/27、`ending_last_month_choice` 16/30、`work_beats_idling` 11/21、`practice_target_receipt` 29/40、`ending_tiers_differentiate` 13/22(首断与实测值见 UX-37/38/39 行与 `99_changelog.md` 收口行)。**复验绿**:RNG 生命线 `save_load_roundtrip` 14/14、`event_travel_effects` 19/19;三条逐字保护闸门全绿(`facility_use_reusable` 49/49、`map_node_event_shaolin` 32/32、`map_battle_node_huashan` 41/41);`spine_to_ending` 42/42、`clicks_only_storyline` 47/47。**本轮关闭:无**——九条红是本轮新钉/重基线场景与 C1 数值落地的待收口项,关闭需各自修复 + 官方复跑 PASS;C5(`huashan_winnable_normal_route` 实测 LOST)的许可红已由本轮动作收口:解锁裁决已授予(2026-09-02,`90_decisions.md` R3b C5 条)、三杠杆落地(`fix_huashan_route_honest_red`)、`huashan_winnable_normal_route` 23/42→29/39、C4 边界绿、**WON 尾仍红(未伪造)**——不以任何 debug/削弱手段伪造胜利,红未收口不撤回裁决。**新开 UX-33..36**(自 `00_roadmap.md` R3b backlog 表收进本队列,record-only)、**UX-37**(R3b 新钉 + 结局家族时间线/表达式缺陷)、**UX-38**(readiness 字面重基线)、**UX-39**(既有回归网两条数值/时序红);UX-09/10/11/12/14/15/19/20..30 保持 OPEN 未动(本轮无指向它们的动作)。C1/C2/C3/C4/C7 的**单元仪器层**(红先四值、M2'/M3' 表)已实测在档,与**场景证据层**(本轮红)分开记,不许互相冒充。证据取自本步上下文中的本轮 `5_compile`(`compile_report.json` / `playtest_summary.md`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。

- 2026-09-02 `R3b numbers-bind`(post-fix 官方复跑收口,5_design 证据步骤):修复轮(fix_scenario_boot_rebaseline / fix_gate_i18n_ending_copy / fix_readiness_verdict_rebaseline→STOP-and-report / fix_huashan_route_honest_red / r3c 三卡)之后的官方 93 场景复跑实测抵达——编译 **107/107** 零错误零警告;视觉闸门 **passed 非盲**(`localqwen/qwen3`,93 场景 372 帧,六问全部 `failed: false`,Q6 **93 好 / 0 坏**);playtest 硬闸门 **`passed: true`**(`spec_used: true`、180 帧、**零 runtime error**)——**91/93 场景零断言失败 / 2 advisory 红**。run #1 九红中**八条转绿**:`cultivation_changes_combat` **30/30**、`action_yield_differential` **44/44**、`huashan_readiness_warning` **16/16**、`ending_divergent_playstyles` **33/33**、`ending_last_month_choice` **38/38**、`work_beats_idling` **26/26**、`practice_target_receipt` **43/43**、`ending_tiers_differentiate` **27/27**;C5 `huashan_winnable_normal_route` **36/48**(WON 尾许可红不变:f1200 相位观测 `ENEMY_TURN`、f2100 `current_state` 观测 `LOST`——按 R3c WIN 裁决不冒充实测 WIN)。**本轮关闭:UX-37 / UX-38 / UX-39 → CLOSED(R3b numbers-bind)**(证据 = `playtest_summary.md` 上述数字);**新开 UX-40(OPEN)**:`trait_combat_effects_and_twelve_slots` **21/22**(f885 `moves_left == 0` 实测 3;run #1 全绿之列、本轮首红,诊断线索指向 C5 解锁杠杆 ③ 的 derive_stats move_range,根因未定不冒充)。C5 WON 尾维持许可红、不撤回解锁裁决、不以任何 debug/削弱手段伪造胜利。UX-33..36 与 UX-09/10/11/12/14/15/19/20..30 保持 OPEN 未动。诚实边界:pytest 契约冒烟与 GDScript 单元套件的官方 PASS 仍以 5_test 产物(`test_report.json`)为准——不在本步上下文,不冒充实测;`40_progression.md` M3'/M2' 节官方状态注记与 `00_roadmap.md` 队列第 0 项同步刷新。证据取自本步上下文中的本轮 `5_compile`(`compile_report.json` / `playtest_summary.md`)与 `5_vision`(`vision_report.json`)产物,非 `final/` 仓库文件。

- 2026-09-03 `R3b numbers-bind`(官方复跑收口,5_design 证据步骤):post-iteration-4 树的官方 93 场景复跑实测抵达(本步上下文中的本轮 `5_compile` / `5_vision` 产物)——编译 **107/107** 零错误零警告;视觉闸门 **passed 非盲**(`localqwen/qwen3`,93 场景 372 帧,六问全部 `failed: false`,Q6 **93 好 / 0 坏**);playtest 硬闸门 **`passed: true`**(`spec_used: true`、180 帧、**零 runtime error**)——**93/93 场景零断言失败、零红**。iteration-4 的两条 sidecar 绿由官方运行实证:`trait_combat_effects_and_twelve_slots` **22/22**(差分钉 `moves_left == turn_start_moves_left - 2`)与 `huashan_winnable_normal_route` **47/47**(诚实-LOST 钉 `current_state == "LOST"` / `health < max_health` / `RetryButton.visible`,按所有者 2026-09-03 重划裁决,C5 的 R3b 交付物在官方树收口、WIN 携 36/48 基线移出 R3b 入 world-breadth 轮)。**本轮关闭:UX-40 → CLOSED(R3b numbers-bind)**(证据 = `playtest_summary.md: trait_combat_effects_and_twelve_slots 22/22`)。上一行(2026-09-02 post-fix 收口)所记 UX-40(OPEN)与「93-scenario official re-run of this tree is the remaining gate」待验欠账以本行收口;遗留 cosmetic / doc-drift 两项(`playtest/huashan_readiness_warning.yaml:125` 注释仍引旧带算术 `< even=38`、`battle_setup.gd:47` 注释 `floor(mp/3)` vs 代码 `:64` `floor(mp/2)`)保持 record-only,留给下一轮 code-touching 步,本轮零代码动作。UX-33..36 与 UX-09/10/11/12/14/15/19/20..30 保持 OPEN 未动(本轮无指向它们的动作)。

`5_vision` 这一轮是**瞎的**:视觉判官 `localqwen/qwen3`
(`http://100.68.74.107:8000`)连接被拒,260 帧一帧未判。闸门**正确地拒绝通过**
(`blind: true, passed: false`,不是「没跑到所以算过」),`5_vision_judged` 于是转人工。

人工判读是**抽样**,不是复审:看了 260 帧中的 4 帧(s11 战斗、s16 属性页与特质页)。
结论是**本轮未引入视觉回归**,故放行;以下是抽样中看到的**既有欠账**,与本轮 brief
(交互缺陷 B/C)无关,记在这里等立项。

### 战斗屏:好

山水背景、六名角色 96×128 立绘、名牌与血条浮于头顶、招式栏满格。
**「美术仍是色块」这句已彻底不成立**,`00_roadmap.md` 第 4 阶段的描述已相应更新。

### 创建角色屏:两页都差,而这是新玩家看到的第一屏

1. **属性页的 ± 按钮竖直方向不成列。** 每行作为整体居中,而标签宽度不同
   (`Bone 20` / `Agility 10` / `Fortune 10`),按钮因此左右参差——实测 `-` 的 x 在
   470~487 之间跳。行与行之间没有对齐轴,眼睛要重新找一次。
2. **特质页 12 条只有名字与数字,没有任何介绍区域。** 这是玩家报告的缺陷 C 的另一面:
   本轮做了 `trait_hover_preview`,但抽到的帧里没有描述面板出现,需要在下一轮用一帧
   悬停态确认它真的会显示(本轮的 `creation_hover_scenario` 断言了行为,没有断言可见)。
3. **两页都是居中细长列,左右各空约三分之一、下方空约四分之一。** 960×704 的画布用了
   不到一半。
4. 属性页的说明是一段挤在一起的长句(`Bone:HP = Bone × 5 · Qi:Qi pool = ...`),
   没有分行分组。

### R4 收尾记录行(2026-09-04 R5 收尾时移入,逐字保留)

> 移入原因:`40_ux_backlog.md` 的 per-file 预算钉(Pin C ≤ 20,480 B)在 R5 新增
> UX-41 行与 2026-09-04 记录行后越界;按 R4 Card N+2 同款纪律,把已收口的旧轮
> 记录行逐字搬入本归档,现场文件留一行指针。

- 2026-09-03 `R4 外号轮`(post-gate 收尾,5_design 证据步骤;证据取自本步上下文中的本轮 `5_compile` / `5_vision` 产物,非 `final/` 仓库文件):本轮官方闸门实测——编译 **109/109** 零错误;playtest **95/95 场景全 PASS**(硬闸门 `passed: true`、`spec_used: true`、180 帧、零 runtime error),改名后两条字面量闸门 `round_one_snapshot_and_turn_order` **14/14**、`ui_geometry_readability` **38/38**,Card 0 新钉 `enemy_turn_wall_clock` **5/5**、`enemy_action_feedback` **9/9**、相机后段遮挡 `camera_transform_follows_unit` **13/13**、`occlusion_no_button_over_text` **22/22**;视觉闸门 passed 非盲(`localqwen/qwen3`,95 场景 380 帧,六问全部 `failed: false`,Q6 **77 好 / 18 坏**——坏答所见理由均为屏底边缘文字被切,无一条指名顺序条或名牌截断)。**本轮关闭:无、新开:无**,逐项裁定:UX-36 修复已落(R4 外号轮别名补漏 + 改名,行内已注记)但无场景钉屏上侠客虾/陪练虾,按规矩 2 保持 OPEN 待一条屏上场景钉;UX-15(资源去名化)保持 OPEN——R4 只改显示层,`assets/seed_manifest.json` 资产清单项在 denylist 范围外;UX-19 的 `侠客` 两条命中被外号轮顺带解决(行内已注记),其余人形泛词未动、保持 OPEN;UX-34/UX-35 与视觉 Q6 的 18 条屏底截断坏答同域,按规矩 3 归入其下待人工筛,不另开卡;其余 OPEN 项本轮无指向动作,状态不变。所有者 2026-09-02 试玩六条反馈按 roadmap 卡(record-only)落在 `00_roadmap.md` backlog/队列(R5/R6),按规矩 3 不自动开卡、待人工筛;Card 0(敌方回合 20–40 s/人,2026-09-02 owner 试玩发现)经 L1 卡直接修复并以 `enemy_turn_wall_clock` 5/5 钉住,不再入列。

### 一条与语言有关的观察

抽样帧里满屏英文中夹着一个中文「退回」按钮(战斗屏右侧)。i18n 有覆盖缺口——
该帧拍摄于语言修复之前,但**未翻译的串会以这种方式混排**这一点与语言设置无关,
下一轮值得做一次覆盖率清点。
