# Real grant → click equip → real encounter → changed → unequip → baseline.
#
# THE ROUND'S CORE DIFFERENTIAL NAIL (jinyong-equipment-battle). Three legs
# through a REAL encounter (battlefield.gd:651 → BattleSetup.build_character
# → derive_stats with EquipmentData.sum_bonuses):
#
#   Leg 1: UNEQUIPPED battle — assert gear_attack_bonus == 0 (and the other
#          three gear mirrors == 0), max_health > 0 (registers observation
#          for `changed`).
#   Leg 2: EQUIPPED battle — after clicking EquipButton0 through the panel,
#          re-travel to huashan; assert gear_attack_bonus changed AND > 0
#          (0 → +6 from eq_sword_3 tier 3 × ATTACK_PER_TIER 2).
#   Leg 3: UNEQUIPPED again — after clicking EquipButton0 to unequip,
#          re-travel; assert gear_attack_bonus == 0 AND changed (reversed).
#
# REAL CODE PATH: eq_sword_3 enters inventory via the merchant event
# (EventLogic.apply_option_effects, the same path as roster_panel_item_nail).
# The player equips it via a click on EquipButton0 (true hit test on the
# real control, never *_ClickTarget). The battle reads equipped gear through
# BattleSetup.build_character → derive_stats → EquipmentData.sum_bonuses.
#
# ROUTE: full boot (menu → creation → tutorial → cultivation → map) reusing
# the proven map_battle_node_huashan frame sequence. The map topology:
#   wuming_valley → luoyang → shaolin → huashan
# After each battle win (debug_win_tutorial → WON → ui_accept → MAP), the
# player is at huashan (current_node_id persists). Re-travel requires going
# to shaolin first (huashan's only neighbor), then back to huashan.
#
# MEASURED ROUTE NOTE (2026-08-31): the post-battle return node after
# debug_win_tutorial → WON → ui_accept is the MAP segment at huashan
# (battle_return_state == "MAP" set by start_map_battle, game_manager.gd:186).
# Shaolin fires its night_rain event on every arrival (events_resolved_count
# increments each pass). The re-travel steps are derived from the adjacency
# table: huashan: [shaolin], shaolin: [luoyang, huashan].
#
# FRAME RE-PROJECTION (2026-08-31, fix_equipment_battle_diff_frame_timing):
# the win→MAP legs are re-projected with WIDE parse/transition windows (spread
# ui_accept + an in-window `current_state == "WON" or current_state == "MAP"`
# diagnostic) and the re-travel legs carry focus_id/phase diagnostics before
# every travel accept. This layout is a PROJECTION (derivation + wide buffer),
# NOT a measured value: the battle→WON→MAP duration could not be measured from
# this step's working dir (godot_playtest_scenario: "No project.godot at /app").
# The exact transition duration and re-travel landing are pending confirmation
# by the 5_compile gate's per-frame report.
#
# KNOWN CODE-LEVEL BLOCKER (must not be hidden by timing): the MAP (huashan)
# battle reuses the TUTORIAL battlefield — battlefield._ready branches to the
# tutorial path for any battle_return_state != "CULTIVATION" (battlefield.gd
# :69-71), instantiating Yang Guo (:80, cd.max_health = 1000) with the tutorial
# overlay. Equipped gear flows into the player ONLY via the CULTIVATION
# encounter (battlefield.gd:651 BattleSetup.build_character(SaveManager.profile)
# in _setup_encounter_battle). Therefore `Player.gear_attack_bonus` is 0 in the
# MAP battle regardless of what the profile carries, so the Leg-2 `> 0` and the
# Leg-3 `changed` asserts CANNOT be satisfied through this route by any frame
# layout. This scenario pins the frame/step mechanics and the panel equip; the
# gear-diff-into-battle proof requires routing through a CULTIVATION encounter
# (debug_enter_encounter) — reported in final/delivery_notes_equipment.md.
#
# NO absolute tuned values: all gear diffs are `changed` or relational
# (`> 0`, `== 0`). The `== 0` baseline is the same shape as
# facility_id == "" (zero-baseline relation, sanctioned).
name: equipment_in_battle_diff
description: >-
  Equipment in battle differential: real merchant grant of eq_sword_3,
  click-to-equip through the roster panel, three encounter legs showing
  gear_attack_bonus 0 → >0 (changed) → 0 (changed, reversed). All through
  the real code path (EventLogic → profile.inventory → profile.equipped →
  BattleSetup.build_character → derive_stats).
timeline:
# ── Leg 0: Boot to MAP (proven huashan route) ──────────────────────────
- at: 3
  actions:
  - ui_accept
- at: 5
  actions:
  - ui_accept
- at: 7
  actions:
  - ui_accept
- at: 9
  actions:
  - ui_accept
- at: 11
  actions:
  - ui_accept
- at: 13
  actions:
  - ui_accept
- at: 15
  actions:
  - ui_accept
- at: 20
  actions:
  - debug_win_tutorial
- at: 50
  actions:
  - ui_accept
- at: 80
  actions:
  - ui_accept
- at: 90
  actions:
  - ui_accept
- at: 120
  actions:
  - move_right
- at: 130
  actions:
  - move_right
- at: 140
  actions:
  - move_right
- at: 150
  actions:
  - move_right
- at: 160
  actions:
  - move_right
- at: 170
  actions:
  - ui_accept
- at: 180
  actions:
  - move_right
- at: 190
  actions:
  - ui_accept
- at: 220
  actions:
  - ui_accept
- at: 280
  actions:
  - debug_fast_forward
- at: 400
  actions: []
  assert:
    GameManager.current_state: current_state == "MAP"
# ── Travel to luoyang (merchant event fires on arrival) ────────────────
- at: 420
  actions:
  - move_right
- at: 430
  actions:
  - ui_accept
- at: 440
  actions: []
  assert:
    MapScreen.phase: phase == "EVENT"
    MapScreen.event_id: event_id == "merchant"
# Resolve merchant: option_a grants eq_sword_3 (real EventLogic path).
- at: 450
  actions: []
  clicks:
  - EventOptionButton0
- at: 460
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
    MapScreen.events_resolved_count: events_resolved_count == 1
# ── Travel to shaolin (luoyang → wudang focus → shaolin focus) ─────────
- at: 470
  actions:
  - move_right
- at: 480
  actions:
  - move_right
- at: 490
  actions:
  - ui_accept
# Shaolin fires night_rain on arrival — resolve it before continuing.
- at: 510
  actions:
  - ui_accept
- at: 520
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
# ── Travel to huashan (shaolin → luoyang focus → huashan focus) ────────
- at: 530
  actions:
  - move_right
- at: 540
  actions:
  - move_right
- at: 550
  actions:
  - ui_accept
# ── Leg 1: BATTLE UNEQUIPPED ───────────────────────────────────────────
# The MAP (huashan) battle reuses the TUTORIAL battlefield: battlefield._ready
# branches to the tutorial path for any battle_return_state != "CULTIVATION"
# (battlefield.gd:69-71, :80 — Yang Guo, not the profile), and
# CombatManager.debug_wipe_enemies no-ops while phase == IDLE (combat_manager.gd
# :454-460), so the tutorial overlay must be advanced (spread ui_accept) before
# the wipe can land. The parse/transition windows below are WIDE with spread
# ui_accept + an in-window diagnostic because the exact battle→WON→MAP duration
# was NOT measurable from this step's working dir (godot_playtest_scenario:
# "No project.godot at /app") — this layout is a projection (derivation + wide
# buffer), pending 5_compile gate confirmation.
- at: 580
  actions: []
  assert:
    GameManager.current_state: current_state == "BATTLE"
    SceneManager.current_scene: current_scene == "battlefield"
    Player.gear_attack_bonus: gear_attack_bonus == 0
    Player.gear_health_bonus: gear_health_bonus == 0
    Player.gear_initiative_bonus: gear_initiative_bonus == 0
    Player.gear_move_bonus: gear_move_bonus == 0
    Player.max_health: max_health > 0
# Advance the tutorial overlay toward a playable phase before the wipe.
- at: 590
  actions:
  - ui_accept
- at: 610
  actions:
  - ui_accept
- at: 630
  actions:
  - ui_accept
- at: 650
  actions:
  - ui_accept
- at: 670
  actions:
  - ui_accept
# Win battle 1 (wipe through the real pipeline → WON).
- at: 690
  actions:
  - debug_win_tutorial
# Wide parse + transition window: spread ui_accept so the first that lands on
# the WON overlay advances WON→MAP (request_continue); the rest are no-ops.
- at: 720
  actions:
  - ui_accept
- at: 750
  actions:
  - ui_accept
- at: 780
  actions:
  - ui_accept
- at: 810
  actions:
  - ui_accept
- at: 840
  actions: []
  assert:
    GameManager.current_state: current_state == "WON" or current_state == "MAP"
# Definite MAP landing with a buffer after the wide window.
- at: 880
  actions: []
  assert:
    GameManager.current_state: current_state == "MAP"
# ── Equip sword through the panel (Leg 2 prep) ─────────────────────────
- at: 890
  actions: []
  clicks:
  - RosterOpenButton
- at: 900
  actions: []
  assert:
    RosterPanel.is_open: is_open == true
    RosterPanel.equipped_weapon: equipped_weapon == ""
    RosterPanel.equip_button_count: equip_button_count >= 1
    RosterPanel.cursor_markers_visible: cursor_markers_visible == false
- at: 910
  actions: []
  clicks:
  - EquipButton0
- at: 920
  actions: []
  assert:
    RosterPanel.equipped_weapon: equipped_weapon == "eq_sword_3"
    RosterPanel.equipped_weapon: changed
    MapScreen.phase: phase == "TRAVEL"
- at: 930
  actions: []
  clicks:
  - RosterCloseButton
- at: 940
  actions: []
  assert:
    RosterPanel.is_open: is_open == false
# ── Re-travel to huashan (Leg 2: equipped) ─────────────────────────────
# After the WON return the map reloads at huashan (current_node_id persists).
# huashan's only neighbor is shaolin; shaolin fires night_rain on arrival
# (resolve before continuing). From shaolin, neighbors are [luoyang, huashan]:
# the proven recipe is move_right×2 → focus huashan → accept. focus_id/phase
# are asserted BEFORE each travel accept (measured-steps precedent): a wrong
# focus names the node instead of failing later with a mystery.
- at: 950
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
    MapScreen.current_node_id: current_node_id == "huashan"
- at: 960
  actions:
  - move_right
- at: 970
  actions:
  - ui_accept
# Resolve shaolin's night_rain entry event on arrival.
- at: 990
  actions:
  - ui_accept
- at: 1010
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
- at: 1020
  actions:
  - move_right
- at: 1030
  actions:
  - move_right
- at: 1040
  actions: []
  assert:
    MapScreen.focus_id: focus_id == "huashan"
- at: 1050
  actions:
  - ui_accept
# ── Leg 2: BATTLE EQUIPPED ─────────────────────────────────────────────
- at: 1080
  actions: []
  assert:
    GameManager.current_state: current_state == "BATTLE"
    Player.gear_attack_bonus: changed
    Player.gear_attack_bonus: gear_attack_bonus > 0
    Player.max_health: max_health > 0
# Advance the tutorial overlay, then win battle 2 → return to MAP.
- at: 1090
  actions:
  - ui_accept
- at: 1110
  actions:
  - ui_accept
- at: 1130
  actions:
  - ui_accept
- at: 1150
  actions:
  - ui_accept
- at: 1170
  actions:
  - ui_accept
- at: 1190
  actions:
  - debug_win_tutorial
- at: 1220
  actions:
  - ui_accept
- at: 1250
  actions:
  - ui_accept
- at: 1280
  actions:
  - ui_accept
- at: 1310
  actions:
  - ui_accept
- at: 1340
  actions: []
  assert:
    GameManager.current_state: current_state == "WON" or current_state == "MAP"
- at: 1380
  actions: []
  assert:
    GameManager.current_state: current_state == "MAP"
# ── Unequip sword through the panel (Leg 3 prep) ───────────────────────
- at: 1390
  actions: []
  clicks:
  - RosterOpenButton
- at: 1400
  actions: []
  assert:
    RosterPanel.is_open: is_open == true
    RosterPanel.equipped_weapon: equipped_weapon == "eq_sword_3"
- at: 1410
  actions: []
  clicks:
  - EquipButton0
- at: 1420
  actions: []
  assert:
    RosterPanel.equipped_weapon: equipped_weapon == ""
    MapScreen.phase: phase == "TRAVEL"
- at: 1430
  actions: []
  clicks:
  - RosterCloseButton
- at: 1440
  actions: []
  assert:
    RosterPanel.is_open: is_open == false
# ── Re-travel to huashan (Leg 3: unequipped again) ─────────────────────
- at: 1450
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
    MapScreen.current_node_id: current_node_id == "huashan"
- at: 1460
  actions:
  - move_right
- at: 1470
  actions:
  - ui_accept
- at: 1490
  actions:
  - ui_accept
- at: 1510
  actions: []
  assert:
    MapScreen.phase: phase == "TRAVEL"
- at: 1520
  actions:
  - move_right
- at: 1530
  actions:
  - move_right
- at: 1540
  actions: []
  assert:
    MapScreen.focus_id: focus_id == "huashan"
- at: 1550
  actions:
  - ui_accept
# ── Leg 3: BATTLE UNEQUIPPED AGAIN (reversibility) ─────────────────────
- at: 1580
  actions: []
  assert:
    GameManager.current_state: current_state == "BATTLE"
    Player.gear_attack_bonus: gear_attack_bonus == 0
    Player.gear_attack_bonus: changed
