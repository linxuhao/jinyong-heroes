# One play-test scenario. Loaded by playtest/_common.yaml's sibling scan —
# scene / actions / surface live there, this file is only the scenario.
# The basename must equal `name:`.
name: dot_resolves_at_victim_turn_start
description: West Poison applies Spirit Serpent poison mid-round 4; the player's round-5 turn start ticks round(8*1.3)=10, masked to a net +16 by the Shen Diao regen (+26).
timeline:
- at: 3
  actions:
  - ui_accept
- at: 5
  actions:
  - ui_accept
- at: 7
  actions:
  - ui_accept
- at: 9
  actions:
  - ui_accept
- at: 11
  actions:
  - ui_accept
- at: 13
  actions:
  - ui_accept
- at: 15
  actions:
  - ui_accept
- at: 20
  actions:
  - move_up
- at: 35
  actions:
  - move_up
- at: 50
  actions:
  - move_up
- at: 65
  actions:
  - skill_1
- at: 80
  actions:
  - attack_confirm
- at: 95
  actions:
  - end_turn
- at: 100
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 150
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 200
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 250
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 300
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 350
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 400
  actions:
  - end_turn
- at: 450
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 500
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 550
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 600
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 650
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 700
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 750
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 800
  actions:
  - end_turn
- at: 850
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 900
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 950
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1000
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1050
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1100
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1150
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1200
  actions:
  - end_turn
- at: 1250
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1300
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1350
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1400
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1450
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1500
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1550
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1600
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1650
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1700
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1750
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
- at: 1800
  actions: []
  assert:
    CombatManager.current_round: current_round >= 1
    CombatManager.phase: phase
    CombatManager.active_unit_name: active_unit_name
    Player.status_names: status_names
    Player.health: health >= 0
